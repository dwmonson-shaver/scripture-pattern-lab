# src/ontology

Node ontology definitions. Contains the type system for query nodes: lemma, concept, root, morphology, domain, and their relationships. See `docs/canonical/04_node-ontology.md` for the design spec.
