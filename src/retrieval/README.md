# src/retrieval

Multi-stage retrieval pipeline. Orchestrates symbolic retrieval, lexical retrieval, semantic retrieval (embeddings), and reranking. Symbolic retrieval is the primary layer; semantic is supporting.
