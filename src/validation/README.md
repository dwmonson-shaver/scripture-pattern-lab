# src/validation

Capability validator. A first-class component (not just a prompt) that checks whether generated queries are executable under current DSL and engine support. Enforces the rule: the system must say "I can't do that yet" rather than fabricate unsupported analysis.
