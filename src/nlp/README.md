# src/nlp

Natural language processing layer. Handles NL-to-DSL translation, query expansion suggestions, result explanation, and counter-evidence generation. The AI assists here but never bypasses the DSL.
