# Spec Coverage Tracker

Last updated: 2026-05-09 (Slice I close — all six MVP routes from canonical-09 §1 now shipped: capabilities, concepts, query/validate, query/dsl, query/nl, health. REQ:09.api-gateway row extended a third time to note the new endpoints. DECs 075–080 cover the slice's load-bearing decisions. Bucket 9 NEW (concepts pagination trigger). Buckets 6 (live-LLM verification), 7 (LLM-in-explainer), 8 (registry-curator runtime invalidation) all re-deferred unchanged.)

## Summary
- Requirements identified: 0
- Implemented: 0 (0%)
- Tested: 0 (0%)
- Both: 0 (0%)
- Neither: 0 (0%)

> Summary numbers reflect the full canonical-doc population once `/coverage` has run. Below, partial rows are populated by hand during `/review` for the REQ markers each phase touches.

## Coverage Matrix

| Req ID | Description | Code | Test | Decision |
|--------|-------------|------|------|----------|
| REQ:08.token-schema | Database schema for the corpus token table | `data/schemas/01_tokens.sql`; `src/ingestion/corpus_parser.py` (`CorpusToken` mirrors all 12 columns); `src/ingestion/db.py` (`tokens_table` Core mirror) | `tests/unit/test_corpus_parser.py::TestParseCorpusLine::test_happy_path_field_by_field`; `tests/integration/test_corpus_ingest.py::test_schema_three_way_consistency` (live SQL ↔ mirror ↔ Pydantic) | DEC-021, DEC-026, DEC-030 |
| REQ:08.ingestion-pipeline | Steps for ingesting MorphGNT data into Postgres | `scripts/db/apply_schemas.sh` (step 4 mechanism); `src/ingestion/corpus_parser.py` (steps 2–3); `src/ingestion/loader.py` (step 4 bulk insert in a single global `engine.begin()` transaction (DEC-044); opt-in `progress_callback` for observability); `src/ingestion/db.py` (`truncate_tokens` reset helper, no self-gate); `scripts/db/ingest_corpus.py` (step 4 production CLI entrypoint with two-factor `--truncate` + `SPL_INGEST_CONFIRM_TRUNCATE=1` gate, exit codes 0/1/2/3, `--corpus-dir` relaxation; default-path filename guard tolerates extras like upstream `README.md` per DEC-048) | `tests/unit/test_corpus_parser.py::TestParseCorpusFile` (steps 2–3 single-file); `tests/unit/test_corpus_parser.py::TestParseCorpusDirectory` (step 3 cross-file: BB-key sort, `global_position` continuity, `position` reset at boundary, count additivity); `tests/unit/test_loader.py::TestProgressCallback` (loader callback semantics: per-batch, file-boundary, post-commit done; `tokens_loaded` = committed count); `tests/integration/test_corpus_ingest.py::test_load_tokens_returns_219`, `test_table_row_count_is_219` (step 4 end-to-end on 3 John, retained as a per-book regression alarm per DEC-045); `tests/integration/test_corpus_ingest.py::test_full_corpus_smoke` (Slice-B exit gate: real script TRUNCATEs and loads all 27 books; asserts exit 0, 27 stderr `file_boundary` lines, `count == EXPECTED_TOKEN_COUNT == 137_554`, `max(global_position) == count`, dup-detector `count(distinct (b,c,v,p)) == count`, 27 distinct books; owns its own truncate boundary per DEC-046; pinned count contract per DEC-047); `tests/integration/test_corpus_ingest.py::test_script_fails_loud_when_tokens_nonempty_without_truncate`, `::test_script_truncate_requires_env_confirm`, `::test_script_redacts_password_in_database_url_print` (step 4 production-CLI gate behavior); `tests/integration/test_apply_schemas.py` (step 4 apply-script idempotency on fresh DB); steps 5–6 pending Slice C | DEC-021, DEC-022, DEC-023, DEC-027, DEC-028, DEC-029, DEC-034, DEC-035, DEC-036, DEC-037, DEC-038, DEC-039, DEC-040, DEC-041, DEC-043, DEC-044, DEC-045, DEC-046, DEC-047, DEC-048 |
| REQ:08.annotation-layers | Per-token surface form, lemma, morph, POS, book, chapter, verse, position | `data/schemas/01_tokens.sql`; `src/ingestion/corpus_parser.py` (all 8 layers populated by `parse_corpus_line`); `src/ingestion/loader.py` (persists all layers via Core insert) | `tests/unit/test_corpus_parser.py::TestParseCorpusLine`, `TestRealCorpusSmoke::test_3jn_first_three_lemmas`; `tests/integration/test_corpus_ingest.py::test_known_row_has_expected_lemma_and_normalized_form` (live-DB persistence) | DEC-026 |
| REQ:08.apparatus-marks | Apparatus marks preserved in `surface_form`, removed from `normalized_form`; queries target `normalized_form` or `lemma` | `data/schemas/01_tokens.sql` (no `surface_form` index); `src/ingestion/corpus_parser.py` (column-3 raw → `surface_form`, column-5 stripped → `normalized_form`) | `tests/unit/test_corpus_parser.py::TestParseCorpusFile::test_apparatus_mark_preserved_in_surface_form`; `tests/integration/test_corpus_ingest.py::test_known_row_has_expected_lemma_and_normalized_form` (asserts `"⸀" not in normalized_form` post-load) | — |
| REQ:05.node-ref | NodeRef AST model with type, value, polarity, morph_filters, negated; types include token/lemma/concept/morph/wildcard | `src/engine/models.py` (`NodeRef`, `NodeType.WILDCARD`); `src/engine/parser.py` (tokenizer emits `*` as WORD; `_parse_typed_value` handles wildcard branch) | `tests/unit/test_parser.py::TestTokenize::test_wildcard_star`; `TestParseNodeRef::test_wildcard`, `test_typed_node`, `test_polarity_*`; `TestParseSequence::test_wildcard_in_sequence` | DEC-033 |
| REQ:05.alternative-expr | AlternativeExpr (`a | b`) — set of options at one step position; supports polarity-on-parens via NodeRef-leaf distribution | `src/engine/models.py` (`AlternativeExpr`); `src/engine/parser.py` (`parse_step` polarity-LPAREN detection; module-level `_distribute_polarity` recursion) | `tests/unit/test_parser.py::TestParseAlternative` (incl. `test_polarity_distributes_to_alternative_options`, `test_minus_polarity_before_alternative`, `test_canonical_05_polarity_alternatives_sequence`) | DEC-031 |
| REQ:06.partial-reduction | Validator must produce a reduced executable plan when status=partial, dropping unsupported features rather than passing them through | `src/validation/validator.py` (`_reduce_step` recurses into AlternativeExpr/GroupExpr/OptionalExpr; `_reduce_sequence` factored out; `_reduce_plan` strips inverse + expansion) | `tests/unit/test_validator.py::TestPartialReduction` (`test_expansion_stripped`, `test_unsupported_node_inside_alternative_collapses`, `test_alternative_dropped_when_all_options_unsupported`, `test_unsupported_inside_optional_dropped`) | DEC-032 |
| REQ:09.ingestion | Corpus ingestion component (non-request-path): parse corpus files, bulk-load tokens, manage schema apply; query-side packages must not import it | `src/ingestion/corpus_parser.py`; `src/ingestion/db.py` (incl. `truncate_tokens` reset helper); `src/ingestion/loader.py` (single global `engine.begin()` transaction per DEC-044; `progress_callback` exposes loader observability without an internal logger); `scripts/db/apply_schemas.sh`; `scripts/db/ingest_corpus.py` (real entrypoint promised by canonical-09; `sys.path` bootstrap pattern keeps `scripts/` non-package; two-factor destructive-op gate; default-path filename guard tolerates extras per DEC-048; `--corpus-dir` relaxation for testing) | `tests/unit/test_corpus_parser.py` (incl. `TestParseCorpusDirectory` cross-file iteration via `_BOOK_NUMBER_BY_FILENAME` monkeypatch); `tests/unit/test_loader.py`; `tests/integration/test_corpus_ingest.py` (incl. 3 production-CLI gate tests covering non-empty refusal, env-confirm pairing, and `_redact_database_url`; `test_full_corpus_smoke` slice-B exit gate per DEC-046/-047); `tests/integration/test_apply_schemas.py` | DEC-021, DEC-025, DEC-028, DEC-034, DEC-037, DEC-038, DEC-039, DEC-040, DEC-041, DEC-042, DEC-043, DEC-044, DEC-046, DEC-047, DEC-048 |
| REQ:08.registry-epistemics | Registry rows carry provenance (`origin`) + verification state; `confidence` defaults to NULL not 1.0; polarity and inverse are evidence-bearing relational claims; `ValidationResult.grounding` axis labels prior-grounded vs evidence-grounded results | `data/schemas/02_concept_registry.sql` (four tables + CHECK constraints on `origin` / `verification_state` / `polarity` / `evidence_count >= 0` / `confidence IS NULL OR 0..1`); `src/ontology/registry.py` (`metadata` + four Table mirrors + `Origin` / `VerificationState` / `Polarity` Literals + `Concept` / `ConceptLemma` / `PolarityClaim` / `InverseClaim` frozen Pydantic + `ConceptRegistry` reader with `get_by_lemma` / `get_polarity_claims` / `get_inverse_claims` / `is_prior_grounded` / `get_lemmas_for_concept` / `empty()`); `src/validation/validator.py` (`ValidationResult.grounding`, `_rule_13_registry_grounding` with `RULE13_PRIOR_GROUNDED` warning code, `validate(plan, capability_registry, concept_registry=None)` signature; rule 13 is additive-not-blocking per DEC-049); `scripts/db/seed_registry.py` (single `engine.begin()` transaction, two-factor `--truncate` gate, `INSERT ... ON CONFLICT DO NOTHING`, every row written with explicit `origin='curated'`, `verification_state='unverified'`, `confidence=NULL`); `data/seeds/registry/{concepts,concept_lemmas,polarity_claims,inverse_claims}.csv` | `tests/unit/test_ontology_registry.py` (27 tests across 5 classes incl. JSON round-trip, frozen-mutation rejection, Literal validation, table-mirror parity); `tests/unit/test_rule_13_registry_grounding.py` (10 tests using `_StubRegistry(ConceptRegistry)` with `engine=None` for no-DB unit coverage; back-compat / unverified / verified / mixed / inverse / non-concept-ignored); `tests/integration/test_apply_schemas.py` (12 tests incl. CHECK-constraint IntegrityError raises for invalid origin / verification_state / polarity / negative evidence_count / out-of-range confidence); `tests/integration/test_concept_registry_reader.py` (14 tests incl. `get_lemmas_for_concept` extension); `tests/integration/test_concept_registry_seed.py` (10 tests including the **Slice C Track 1 exit gate**: `SELECT DISTINCT verification_state FROM <each table>` returns exactly `{'unverified'}` — corpus-is-ground-truth invariant) | DEC-024, DEC-025 (boundary held), DEC-007 (grounding axis), DEC-015 (additive-warning rule), DEC-049 (umbrella for the four invariants), DEC-052 (DEC-025 boundary preserved via `src/engine/_schema.py` once executor lands) |
| REQ:08.polarity-claims-table | Evidence-bearing relational table for polarity claims (one row per concept-pole pair) with origin, evidence_count, verification_state, confidence | `data/schemas/02_concept_registry.sql` (`polarity_claims` with `UNIQUE (concept_id, polarity)` + CHECK constraints on `polarity IN ('+', '-', '±')`, `evidence_count >= 0`, `confidence IS NULL OR 0..1`); `src/ontology/registry.py` (`polarity_claims_table` Core mirror + `PolarityClaim` Pydantic) | `tests/integration/test_apply_schemas.py::test_polarity_claims_unique_per_concept_pole`, `::test_polarity_check_rejects_invalid_value`, `::test_evidence_count_check_rejects_negative_value`; `tests/integration/test_concept_registry_seed.py` (19 polarity rows seeded, all `verification_state='unverified'`); `tests/integration/test_concept_registry_reader.py::test_get_polarity_claims_*` | DEC-024, DEC-049 |
| REQ:08.inverse-claims-table | Evidence-bearing relational table for inverse claims (asymmetric pairs) with origin, evidence_count, verification_state, confidence | `data/schemas/02_concept_registry.sql` (`inverse_claims` with `UNIQUE (concept_id, inverse_concept_id)` + `CHECK (concept_id <> inverse_concept_id)` + value-domain CHECKs); `src/ontology/registry.py` (`inverse_claims_table` Core mirror + `InverseClaim` Pydantic) | `tests/integration/test_apply_schemas.py::test_inverse_claims_self_inverse_check_constraint`; `tests/integration/test_concept_registry_seed.py` (7 inverse-pair rows seeded); `tests/integration/test_concept_registry_reader.py::test_get_inverse_claims_*` | DEC-024, DEC-049 |
| REQ:09.pattern-engine | SQL-based sequence search executor: `execute(plan, scope, engine, concept_registry=None) -> list[MatchCandidate]`; resolves LEMMA + CONCEPT NodeRefs, applies PRECEDENCE operators with optional GapConstraint, honors ScopeConstraint{corpus, language, books, unit=verse}; raises `UnsupportedPlanShape` on out-of-MVP-contract plans (negated nodes, morph_filters, malformed operator counts, AlternativeExpr/OptionalExpr/GroupExpr/InverseExpr at sequence level, non-LEMMA/CONCEPT NodeTypes, non-PRECEDENCE operators, non-verse scope units); raises `RegistryRequired` when a CONCEPT step lacks a registry; raises `ConceptNotMapped` when a CONCEPT resolves to `[]` lemmas | `src/engine/executor.py` (`execute`, `validate_plan_shape`, `resolve_step_lemmas`, `build_scope_where` — public per DEC-059 so `src/retrieval/` can reuse them; `_extend_chains_one_step`, `_gap_satisfied`, `_row_to_matched_token`, `_format_reference` remain private); `src/engine/models.py` (`MatchedToken`, `StepMatch`, `MatchCandidate`, `UnsupportedPlanShape`, `RegistryRequired`, `ConceptNotMapped`); `src/engine/_schema.py` (private `_metadata` + `tokens_table` mirror — DEC-052 boundary fix); `src/ontology/book_codes.py` (abbrev↔BB↔display map; closes Bucket 3); `scripts/query.py` (parse → validate → retrieve → render CLI; exit 0/1/2/3 taxonomy mirroring ingest_corpus.py; calls `retrieve(contextualize=True)` per DEC-056) | `tests/unit/test_executor.py` (14 tests, no DB; covers every UnsupportedPlanShape / RegistryRequired / ConceptNotMapped raise path + scope-filter-on-every-step compile assertions); `tests/unit/test_book_codes.py` (7 tests incl. silent-miss guard `test_canonical_07_examples_all_resolve`); `tests/integration/test_executor.py` (7 tests incl. Slice C exit gate `test_full_corpus_faith_hope_love_in_1cor_13_13`); `tests/integration/test_query_cli.py` (9 tests incl. supported / partial / unsupported / no-matches / password-redaction / parse-error / unknown-concept exit-3 paths + 2 Slice D contextualization-rendering tests) | DEC-014 (engine match-type support), DEC-007 (results distinguish match types), DEC-026 (BB-digit storage), DEC-024 (no silent coercion), DEC-050 (MVP contract: raise on out-of-shape), DEC-051 (Track 2 contextualization deferred to Slice D), DEC-052 (DEC-025 boundary via `src/engine/_schema.py`), DEC-053 (book_codes.py placement), DEC-059 (helpers promoted to public for retrieval-layer reuse) |
| REQ:09.retrieval-pipeline | Multi-stage retrieval orchestrator (canonical-09 §6): `retrieve(plan, scope, engine, *, contextualize=False, registry=None) -> RetrievalResult`. MVP is a thin wrapper around the pattern engine plus an optional contextualization stage; the interface exists so semantic retrieval can be added later as additional stages. Engine-layer default-off / API-CLI default-on per DEC-056. | `src/retrieval/retrieve.py` (`retrieve()`); `src/engine/models.py` (`RetrievalResult` frozen Pydantic with `candidates`, `stages_used`, `contextualization`); `scripts/query.py` (UI-layer consumer; passes `contextualize=True`) | `tests/unit/test_retrieve.py` (5 tests: default-off, contextualize=True envelope, exception propagation, empty-candidate case, registry pass-through); `tests/integration/test_contextualization.py` (2 retrieve()-pipeline tests covering default-off + contextualize=True against the full NT + seeded registry); `tests/integration/test_query_cli.py` (CLI consumes RetrievalResult.contextualization via the slice exit gate) | DEC-024 (corpus-is-ground-truth), DEC-054 (REQ marker), DEC-055 (code home `src/retrieval/`), DEC-056 (default split), DEC-059 (executor helpers promoted) |
| REQ:09.contextualization | Result-set calibration envelope (canonical-09 §8): per-node baselines, alternative-ordering counts, and a reserved null-distribution slot. Hangs on `RetrievalResult.contextualization` (and on `ExplainedResultSet.contextualization` once the explainer slice ships). Four invariants: per-node baselines, alt-ordering counts capped at `min(N!, 24)`, null-distribution slot reserved (always None in MVP per DEC-057), explainer must surface contextualization. | `src/retrieval/contextualization.py` (`compute_node_baselines`, `compute_alternative_orderings`, `contextualize`, private `_fallback_permutations` truncated at `_MAX_PERMUTATIONS=24`, `_format_sequence_label`); `src/retrieval/retrieve.py` (`retrieve(contextualize=True)` consumer — the only producer of populated envelopes today); `src/engine/models.py` (`NodeBaseline`, `AlternativeOrderingCount`, `NullDistribution`, `Contextualization`, `RetrievalResult.contextualization`; `NonNegativeInt` / `NonNegativeFloat` schema-enforced ge=0 invariants per Codex D-D1D2-002); `scripts/query.py::_print_contextualization` (UI-layer rendering: observed count, baselines, alt-orderings sorted by count desc with observed marked *, null-distribution "not computed in MVP") | `tests/unit/test_contextualization.py` (24 tests across baseline / alt-ordering / orchestrator / fallback-permutation / sequence-label classes — covers shape failures, scope language propagation, observed-vs-alternative marking, capped-flag correctness, N=30 boundary truncation); `tests/unit/test_models.py` (5 test classes covering NodeBaseline / AlternativeOrderingCount / NullDistribution / Contextualization / RetrievalResult — frozen, JSON round-trip, negative-value rejection); `tests/integration/test_contextualization.py` (7 tests incl. lemma=direct-COUNT, concept=sum-of-resolved-lemmas, three-step faith>hope>love structural shape, scope filter reduces count, alt-ordering identity matches execute(), retrieve() default-off, retrieve() contextualize=True full-envelope); `tests/integration/test_query_cli.py::test_cli_renders_contextualization_for_flagship_sequence` (**Slice D exit gate**: `faith > hope > love` produces a stdout block with Constituent baselines + Alternative orderings + Null distribution slot); `tests/integration/test_query_cli.py::test_cli_renders_no_match_with_contextualization` (zero-match case still renders the envelope) | DEC-024 (corpus-is-ground-truth — output-side companion), DEC-054 (REQ marker + 4 invariants), DEC-055 (code home), DEC-056 (default split), DEC-057 (null-distribution deferred), DEC-058 (permutation cap policy), DEC-059 (helpers promoted) |
| REQ:04.matching-rules | Node-type → corpus field resolution table (canonical-04 §matching-rules); MVP supports the LEMMA and CONCEPT rows; ROOT / DOMAIN / MORPH / WILDCARD / TOKEN raise `UnsupportedPlanShape` at executor entry | `src/engine/executor.py::resolve_step_lemmas` (public per DEC-059; LEMMA → `[node.value]`; CONCEPT → `concept_registry.get_lemmas_for_concept(name, language)`); `src/engine/executor.py::validate_plan_shape` (public per DEC-059; rejects ROOT/DOMAIN/MORPH/WILDCARD/TOKEN with `UnsupportedPlanShape`); also consumed by `src/retrieval/contextualization.py::compute_node_baselines` (same resolution rules apply at the calibration layer) | `tests/unit/test_executor.py::test_raises_unsupported_on_wildcard_node` (and the broader UnsupportedPlanShape coverage); `tests/integration/test_executor.py::test_concept_step_resolves_via_registry`, `::test_single_lemma_step_returns_matches`; `tests/unit/test_contextualization.py::test_lemma_baseline_returns_self_resolution`, `::test_concept_baseline_uses_registry_resolution`; `tests/integration/test_contextualization.py::test_concept_baseline_equals_sum_of_resolved_lemmas` | DEC-014, DEC-050, DEC-059 |
| REQ:09.result-explainer | Result builder + explainer (canonical-09 §9): `explain(result: RetrievalResult, plan: QueryPlan, validation: ValidationResult) -> ExplainedResultSet` produces deterministic prose grounded in the actual corpus counts. `ExplainedResultSet` carries `query_shown`, optional `nl_source`, `validation_notes` (raw finding strings), `results: list[ExplainedResult]`, optional `contextualization`, and a required `summary` (≤ 5 lines). `ExplainedResult` carries `reference`, `text_display`, `match_type`, optional `score`, `explanation`. Five invariants (canonical §9 (a)–(e)): RetrievalResult-keyed signature; field-derived prose only (no fabrication, DEC-024); deterministic / no LLM / no I/O (DEC-061); validation_notes raw-string format; cap policy in prose layer (lemmas ≤ 5 with "+N more"; sequence labels ≤ 64 chars with ellipsis) — `_print_contextualization` stays unbounded by design. | `src/nlp/explainer.py` (`explain()` orchestrator + private helpers `_summary_prose`, `_per_candidate_prose`, `_format_baselines_phrase`, `_format_alt_orderings_phrase`, `_verse_list_clause`, `_truncate_lemmas`, `_truncate_sequence_label`, `_sequence_label_for_plan`, `_text_display_for_candidate`, `_format_validation_notes`, `_format_finding`); `src/engine/models.py` (`ExplainedResult`, `ExplainedResultSet` frozen Pydantic; `score: float \| None = None` per DEC-061); `scripts/query.py` (CLI integration; `--no-prose` flag; `_print_explanation` helper); **Slice G**: `src/app/routes/query.py` is the second consumer (HTTP), surfacing the deterministic prose unchanged via the `explanation` field of `QueryDSLResponse` | `tests/unit/test_models.py::TestExplainedResult` + `::TestExplainedResultSet` (10 tests: minimal/full construction, Literal validation, summary required, frozen, JSON round-trip); `tests/unit/test_explainer.py` (37 tests across TestExplainFlagship / TestExplainZeroMatch / TestValidationNotes / TestTruncateLemmas / TestTruncateSequenceLabel / TestFormatBaselinesPhrase / TestFormatAltOrderingsPhrase / TestExplainEdgePaths (InverseExpr label + nl_source round-trip + text_display populated/empty) / TestVariantMatchType / TestPoseQualityFixes / TestCappedSummary); `tests/integration/test_query_cli.py::test_cli_renders_explanation_for_flagship_sequence` (**Slice F exit gate**: prose block lands AFTER contextualization with grounded counts/verse/alt-ordering observation); `tests/integration/test_query_cli.py::test_cli_no_prose_flag_suppresses_explanation` (--no-prose suppresses prose; structured + ctx blocks remain); `tests/integration/test_app_dsl_route.py::test_query_dsl_returns_flagship_sequence_envelope` (**Slice G exit gate**: explanation.summary surfaces over HTTP with `len(splitlines()) ≤ 5` invariant) | DEC-015 (AI explains/expands/critiques — honored via deterministic templating); DEC-024 (corpus-is-ground-truth — every prose claim field-derived); DEC-056 (UI default-on — prose ON by default in CLI); DEC-061 (RetrievalResult signature + LLM deferred to Bucket 7); DEC-063 (response envelope composes ExplainedResultSet verbatim) |
| REQ:09.api-gateway | API gateway component (canonical-09 §1): `src/app/` owns HTTP routing, request validation, JSON I/O, typed request/response schemas, dependency injection, and the exception→HTTP-status mapping table. Slice G shipped foundation + DSL route + health. Slice H added `POST /api/v1/query/nl`. **Slice I closes the §1 routes table by shipping the three remaining MVP routes** — `GET /api/v1/capabilities` (returns `CapabilityRegistry.mvp()` directly per DEC-075; engine-free, no DI), `GET /api/v1/concepts` (returns flat `ConceptsResponse{concepts: list[ConceptSummary]}` with embedded lemmas per DEC-076; optional `language` query param; not paginated at MVP scale per Bucket 9), `POST /api/v1/query/validate` (returns `QueryValidateResponse{query, validation}` with `validation.status` carrying the verdict per DEC-079; only 422 path is parse_error). All six MVP routes from canonical-09 §1 are now coded. | `src/app/main.py` (`create_app()` factory + lifespan + module-level `app`); `src/app/dependencies.py` (`get_engine` / `get_concept_registry` providers raising 503 when state is None); `src/app/orchestration.py` (`run_dsl_query()` pipeline helper + `ValidationUnsupported`); `src/app/schemas.py` (`QueryDSLRequest`, `QueryDSLResponse`, `ErrorResponse`); `src/app/routes/__init__.py` (package marker); `src/app/routes/health.py` (`GET /api/v1/health`); `src/app/routes/query.py` (`POST /api/v1/query/dsl` handler + exception→HTTP-status `try/except` chain) | `tests/unit/test_app_schemas.py` (14 tests: request rejection of empty/missing dsl, frozen, JSON round-trip, null emission for ErrorResponse.details and nested nullable fields); `tests/unit/test_app_orchestration.py` (8 tests across parse-error, validator-rejects-inverse, retrieve-side exception propagation, happy-path with mocked retrieve, partial-validation path); `tests/unit/test_app_main.py` (8 tests: factory isolation, lifespan with unset DATABASE_URL, lifespan with mocked engine builder, dependency_overrides supersede state lookup, 503 fallback when state is unset and no override); `tests/unit/test_app_health.py` (4 tests: 200, status body, JSON content-type, no-DB invariant); `tests/unit/test_app_routes.py` (13 tests: request body validation, every error→status mapping, happy path 200, null emission, partial-validation 200 with findings, route registration); `tests/integration/test_app_dsl_route.py` (5 tests: live `/health` against the live app; **Slice G exit gate** `test_query_dsl_returns_flagship_sequence_envelope` — JSON envelope shape, baselines 483/84/259, 6 alt-orderings, `null_distribution: null`, summary ≤ 5 lines; plus three sad-path live-DB integration assertions for parse_error/validation_unsupported/concept_not_mapped) | DEC-024 (corpus is ground truth — response surfaces structured findings + raw counts, not prose-only); DEC-025 (engine⊥ingestion boundary — `src/app/main.py` reuses CLI's `src.ingestion.db.get_engine` carve-out); DEC-052 (cross-layer-narrowness precedent); DEC-056 (engine-default-False / API-CLI-default-True for `contextualize`); DEC-062 (factory + lifespan + DI architecture); DEC-063 (response envelope); DEC-064 (HTTP status code mapping); DEC-065 (null-field emission policy / OQ #6 resolution); DEC-066 (health-only + endpoint deferrals) |
| REQ:09.nl-to-dsl | NL→DSL translator (canonical-09 §2): single-shot LLM call returning a `TranslationResult{dsl, confidence, alternatives, explanation}` per the canonical interface. `LLMClient` is a concrete base class (DEC-067, not Protocol) with `.complete(system, user) -> str`; `AnthropicLLMClient` is the sole concrete child for MVP. System prompt assembled at module import time from `docs/agent/dsl-cookbook.md` plus a compile-only translator framing (DEC-071). Translator output extracted via line-anchored regex against a `DSL: <string>` line plus optional `Confidence:`, `Alternatives:`, `Explanation:` lines; missing/empty `DSL:` raises `NLCompileError`. Failure-mode wrapping in `AnthropicLLMClient.complete` is narrowed to availability + auth + 5xx server exceptions (DEC-070, H-H1H2-001) — 4xx code-bug exceptions propagate raw. No confidence-threshold gate (DEC-072: corpus-is-ground-truth charter). | `src/nlp/llm_client.py` (`LLMClient`, `AnthropicLLMClient`, `LLMUnavailable`, `build_anthropic_client_from_env`); `src/nlp/translator.py` (`translate`, `TranslationContext`, `TranslationResult`, `NLCompileError`, 5 line-anchored regex helpers); `src/nlp/prompts/system_prompt.py` (`SYSTEM_PROMPT` module constant + `build_system_prompt`); `src/app/schemas.py` (`QueryNLRequest`, `QueryNLResponse(QueryDSLResponse)`, `TranslationMetadata`); `src/app/orchestration.py::run_nl_query` (composes `translate` + `run_dsl_query`); `src/app/dependencies.py` (`get_llm_client`, `get_translation_context`); `src/app/main.py` (lifespan reads `ANTHROPIC_API_KEY`, builds `LLMClient` + `TranslationContext` independently from DB); `src/app/routes/nl.py` (`POST /api/v1/query/nl` handler + 8-branch exception mapping per DEC-070). | `tests/unit/test_llm_client.py` (17 tests: base class abstract behavior, construction with/without API key, happy-path + text-block filtering, narrowed error wrapping for availability + auth + 5xx + permission, parameterized 4xx-propagate-raw set, build-from-env behavior); `tests/unit/test_translator.py` (19 tests: system prompt assembly + cookbook embed + framing, output parsing happy/missing/empty/malformed/asterisk-bullets, default-value behavior, LLMUnavailable propagation, frozen invariants); `tests/unit/test_app_orchestration.py::TestRunNLQueryHappyPath` + `::TestRunNLQueryTranslatorFailures` + `::TestRunNLQueryDownstreamFailures` (6 tests); `tests/unit/test_app_schemas.py::TestQueryNLRequest` + `::TestTranslationMetadata` + `::TestQueryNLResponseInheritance` (6 tests covering construction, frozen, subclass relation); `tests/unit/test_app_routes_nl.py` (11 tests across happy-path, request validation, translator-side errors, every downstream pipeline error, provider-503-when-state-missing); `tests/unit/test_app_main.py::TestLifespanWithAnthropicApiKey` (2 tests: independent ANTHROPIC_API_KEY branch + DB-set-but-LLM-unset degradation); `tests/integration/test_app_nl_route_live_llm.py` (**Slice H exit gate** + ambiguity-surfacing test; gated by `live_llm` + `integration` markers, requires `DATABASE_URL` + `ANTHROPIC_API_KEY` to run). | DEC-003 (NL compiles to DSL, never bypass), DEC-006 (system says when it cannot do something), DEC-024 (corpus is ground truth — no confidence-threshold gating), DEC-061 (deterministic-first; LLM-prose-explainer is Bucket 7), DEC-067 (concrete LLMClient, not Protocol), DEC-068 (`live_llm` pytest marker), DEC-069 (`QueryNLResponse(QueryDSLResponse)` subclass envelope), DEC-070 (HTTP error mapping for translator failures + narrowed wrapping per H-H1H2-001), DEC-071 (system prompt assembled from cookbook at import time), DEC-072 (no confidence-threshold gate), DEC-073 (alternatives surfaced not auto-retried), DEC-074 (lifespan-scoped LLMClient + TranslationContext via `get_llm_client` / `get_translation_context` providers) |
| REQ:09.request-lifecycle | The 11-step ordered pipeline from "request hits FastAPI" through "response leaves FastAPI" (canonical-09 §request-lifecycle). For `POST /api/v1/query/dsl` step 3 (NL→DSL) is skipped per the spec note. **Slice H closes the lifecycle on the NL path**: step 3 (NL→DSL translator calls LLM, returns DSL string + explanation) is now coded via `src/app/orchestration.py::run_nl_query`. Slice G + H together realize all canonical lifecycle steps that apply to MVP (step 9 — scoring — remains a no-op per REQ:09.scoring-ranking gap). | `src/app/routes/query.py::post_query_dsl` (steps 1–2 = FastAPI body validation; step 4 via `src/engine/parser.py::parse`; step 5 via `src/validation/validator.py::validate`; step 5a/5b/5c branch via `ValidationUnsupported` + partial-status pass-through in `run_dsl_query`; steps 6–8 via `src/retrieval/retrieve.py::retrieve(contextualize=True)`; step 10 via `src/nlp/explainer.py::explain`; step 11 returns the `QueryDSLResponse` JSON envelope); `src/app/orchestration.py::run_dsl_query` (the in-process orchestrator that threads the executable plan through retrieve + explain) | `tests/unit/test_app_orchestration.py` (mocks each pipeline stage to assert step ordering and exception propagation match `scripts/query.py::main`); `tests/unit/test_app_routes.py::TestErrorMapping` (steps 4 / 5 / 6 each exercised via their respective exception paths); `tests/integration/test_app_dsl_route.py::test_query_dsl_returns_flagship_sequence_envelope` (full end-to-end on the real corpus: all 8 lifecycle steps that apply to `/dsl` execute and surface in the JSON envelope; step 9 — scoring — is still a no-op per REQ:09.scoring-ranking gap) | DEC-024 (corpus is ground truth — the full lifecycle's transparency invariants now apply to HTTP consumers too); DEC-061 (RetrievalResult signature for explainer); DEC-062 (factory + lifespan + DI architecture); DEC-063 (response envelope); DEC-064 (HTTP status mapping for the lifecycle's error responses) |

_Run `/coverage` to populate this table from `<!-- REQ:... -->` markers in `docs/canonical/`._

## Gaps

### Specced but not coded
- **REQ:09.scoring-ranking**: canonical-09 §7 boundary clarified at Slice D close (DEC-054); no implementation. `src/scoring/` is empty. Lands when scoring slice begins.
- _Closed 2026-05-09 (Slice G): REQ:09.request-lifecycle gap — the 11-step lifecycle now has a coverage row above; the as-shipped HTTP flow realizes it end-to-end._

_Slice-D-tracked drift (REQ:04.matching-rules pre-Slice-D underscored helpers) closed at Slice D close. Slice-F-tracked drift (REQ:09.result-explainer pre-Slice-F `list[ScoredMatch]` signature + LLM-conceptual claim) closed by canonical-09 amendment + DEC-061._

### Coded but not tested
_None tracked yet — pending `/coverage` run for full audit. (Closed 2026-05-03: `REQ:08.ingestion-pipeline` step 3 cross-file gap — `tests/unit/test_corpus_parser.py::TestParseCorpusDirectory` now covers BB-key sort, `global_position` continuity, `position` reset at file boundary, and count additivity via a 2-book real-format fixture; commit `83685a4` / DEC-037.)
