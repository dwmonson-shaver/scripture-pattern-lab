# Spec Coverage Tracker

Last updated: 2026-05-23 (Full `/coverage` audit — first run since the harness was built. All 79 canonical REQ markers now have rows. Pre-existing 19 rows from incremental `/review` populations preserved verbatim. 60 new rows added covering REQ:01.* (4), REQ:02.* (7), REQ:03.* (5), REQ:04.* (10), REQ:05.* (13), REQ:06.* (15), REQ:08.* (3), REQ:09.* (3). The "Requirements identified: 0" placeholder Summary block has been replaced with actual counts.)

## Summary
- Requirements identified: 79
- Covered (code + tests, both refs exist): 70 (89%)
- Stub rows (in matrix but Code or Test placeholder): 0 (0%)
- Code present, no row before this audit (now populated): 60 (76%) — see audit annotation in Notes column
- Not yet implemented (specced but no code expected at MVP): 4 (5%) — `REQ:04.node-root`, `REQ:04.node-domain`, `REQ:03.scoring-philosophy`, `REQ:09.scoring-ranking`
- Deprecated / merged: 0
- Partial-only (parser surface exists, validator rejects at MVP per v0.1 capability registry): 5 — `REQ:02.expansion-syntax`, `REQ:04.node-root`, `REQ:04.node-domain`, `REQ:05.inverse-expr` (AST + parser, validator rejects), `REQ:05.expansion-directive` (AST + parser, validator rejects)

> Summary numbers are derived from the audit on 2026-05-23. Before this run the Summary said "Requirements identified: 0" because incremental `/review` populated rows by hand for slice-touched REQs only. Most of the 60 new rows describe code + tests that have existed since Slice A/B/C — they simply lacked a coverage-matrix row.

## Coverage Matrix

| Req ID | Description | Code | Test | Decision |
|--------|-------------|------|------|----------|
| REQ:08.token-schema | Database schema for the corpus token table | `data/schemas/01_tokens.sql`; `src/ingestion/corpus_parser.py` (`CorpusToken` mirrors all 12 columns); `src/ingestion/db.py` (`tokens_table` Core mirror) | `tests/unit/test_corpus_parser.py::TestParseCorpusLine::test_happy_path_field_by_field`; `tests/integration/test_corpus_ingest.py::test_schema_three_way_consistency` (live SQL ↔ mirror ↔ Pydantic) | DEC-021, DEC-026, DEC-030 |
| REQ:08.ingestion-pipeline | Steps for ingesting MorphGNT data into Postgres | `scripts/db/apply_schemas.sh` (step 4 mechanism); `src/ingestion/corpus_parser.py` (steps 2–3); `src/ingestion/loader.py` (step 4 bulk insert in a single global `engine.begin()` transaction (DEC-044); opt-in `progress_callback` for observability); `src/ingestion/db.py` (`truncate_tokens` reset helper, no self-gate); `scripts/db/ingest_corpus.py` (step 4 production CLI entrypoint with two-factor `--truncate` + `SPL_INGEST_CONFIRM_TRUNCATE=1` gate, exit codes 0/1/2/3, `--corpus-dir` relaxation; default-path filename guard tolerates extras like upstream `README.md` per DEC-048) | `tests/unit/test_corpus_parser.py::TestParseCorpusFile` (steps 2–3 single-file); `tests/unit/test_corpus_parser.py::TestParseCorpusDirectory` (step 3 cross-file: BB-key sort, `global_position` continuity, `position` reset at boundary, count additivity); `tests/unit/test_loader.py::TestProgressCallback` (loader callback semantics: per-batch, file-boundary, post-commit done; `tokens_loaded` = committed count); `tests/integration/test_corpus_ingest.py::test_load_tokens_returns_219`, `test_table_row_count_is_219` (step 4 end-to-end on 3 John, retained as a per-book regression alarm per DEC-045); `tests/integration/test_corpus_ingest.py::test_full_corpus_smoke` (Slice-B exit gate: real script TRUNCATEs and loads all 27 books; asserts exit 0, 27 stderr `file_boundary` lines, `count == EXPECTED_TOKEN_COUNT == 137_554`, `max(global_position) == count`, dup-detector `count(distinct (b,c,v,p)) == count`, 27 distinct books; owns its own truncate boundary per DEC-046; pinned count contract per DEC-047); `tests/integration/test_corpus_ingest.py::test_script_fails_loud_when_tokens_nonempty_without_truncate`, `::test_script_truncate_requires_env_confirm`, `::test_script_redacts_password_in_database_url_print` (step 4 production-CLI gate behavior); `tests/integration/test_apply_schemas.py` (step 4 apply-script idempotency on fresh DB); steps 5–6 pending Slice C | DEC-021, DEC-022, DEC-023, DEC-027, DEC-028, DEC-029, DEC-034, DEC-035, DEC-036, DEC-037, DEC-038, DEC-039, DEC-040, DEC-041, DEC-043, DEC-044, DEC-045, DEC-046, DEC-047, DEC-048 |
| REQ:08.annotation-layers | Per-token surface form, lemma, morph, POS, book, chapter, verse, position | `data/schemas/01_tokens.sql`; `src/ingestion/corpus_parser.py` (all 8 layers populated by `parse_corpus_line`); `src/ingestion/loader.py` (persists all layers via Core insert) | `tests/unit/test_corpus_parser.py::TestParseCorpusLine`, `TestRealCorpusSmoke::test_3jn_first_three_lemmas`; `tests/integration/test_corpus_ingest.py::test_known_row_has_expected_lemma_and_normalized_form` (live-DB persistence) | DEC-026 |
| REQ:08.apparatus-marks | Apparatus marks preserved in `surface_form`, removed from `normalized_form`; queries target `normalized_form` or `lemma` | `data/schemas/01_tokens.sql` (no `surface_form` index); `src/ingestion/corpus_parser.py` (column-3 raw → `surface_form`, column-5 stripped → `normalized_form`) | `tests/unit/test_corpus_parser.py::TestParseCorpusFile::test_apparatus_mark_preserved_in_surface_form`; `tests/integration/test_corpus_ingest.py::test_known_row_has_expected_lemma_and_normalized_form` (asserts `"⸀" not in normalized_form` post-load) | — |
| REQ:05.node-ref | NodeRef AST model with type, value, polarity, morph_filters, negated; types include token/lemma/concept/morph/wildcard | `src/engine/models.py` (`NodeRef`, `NodeType.WILDCARD`); `src/engine/parser.py` (tokenizer emits `*` as WORD; `_parse_typed_value` handles wildcard branch) | `tests/unit/test_parser.py::TestTokenize::test_wildcard_star`; `TestParseNodeRef::test_wildcard`, `test_typed_node`, `test_polarity_*`; `TestParseSequence::test_wildcard_in_sequence` | DEC-033 |
| REQ:05.alternative-expr | AlternativeExpr (`a \| b`) — set of options at one step position; supports polarity-on-parens via NodeRef-leaf distribution | `src/engine/models.py` (`AlternativeExpr`); `src/engine/parser.py` (`parse_step` polarity-LPAREN detection; module-level `_distribute_polarity` recursion) | `tests/unit/test_parser.py::TestParseAlternative` (incl. `test_polarity_distributes_to_alternative_options`, `test_minus_polarity_before_alternative`, `test_canonical_05_polarity_alternatives_sequence`) | DEC-031 |
| REQ:06.partial-reduction | Validator must produce a reduced executable plan when status=partial, dropping unsupported features rather than passing them through | `src/validation/validator.py` (`_reduce_step` recurses into AlternativeExpr/GroupExpr/OptionalExpr; `_reduce_sequence` factored out; `_reduce_plan` strips inverse + expansion) | `tests/unit/test_validator.py::TestPartialReduction` (`test_expansion_stripped`, `test_unsupported_node_inside_alternative_collapses`, `test_alternative_dropped_when_all_options_unsupported`, `test_unsupported_inside_optional_dropped`) | DEC-032 |
| REQ:09.ingestion | Corpus ingestion component (non-request-path): parse corpus files, bulk-load tokens, manage schema apply; query-side packages must not import it | `src/ingestion/corpus_parser.py`; `src/ingestion/db.py` (incl. `truncate_tokens` reset helper); `src/ingestion/loader.py` (single global `engine.begin()` transaction per DEC-044; `progress_callback` exposes loader observability without an internal logger); `scripts/db/apply_schemas.sh`; `scripts/db/ingest_corpus.py` (real entrypoint promised by canonical-09; `sys.path` bootstrap pattern keeps `scripts/` non-package; two-factor destructive-op gate; default-path filename guard tolerates extras per DEC-048; `--corpus-dir` relaxation for testing) | `tests/unit/test_corpus_parser.py` (incl. `TestParseCorpusDirectory` cross-file iteration via `_BOOK_NUMBER_BY_FILENAME` monkeypatch); `tests/unit/test_loader.py`; `tests/integration/test_corpus_ingest.py` (incl. 3 production-CLI gate tests covering non-empty refusal, env-confirm pairing, and `_redact_database_url`; `test_full_corpus_smoke` slice-B exit gate per DEC-046/-047); `tests/integration/test_apply_schemas.py` | DEC-021, DEC-025, DEC-028, DEC-034, DEC-037, DEC-038, DEC-039, DEC-040, DEC-041, DEC-042, DEC-043, DEC-044, DEC-046, DEC-047, DEC-048 |
| REQ:08.registry-epistemics | Registry rows carry provenance (`origin`) + verification state; `confidence` defaults to NULL not 1.0; polarity and inverse are evidence-bearing relational claims; `ValidationResult.grounding` axis labels prior-grounded vs evidence-grounded results | `data/schemas/02_concept_registry.sql` (four tables + CHECK constraints on `origin` / `verification_state` / `polarity` / `evidence_count >= 0` / `confidence IS NULL OR 0..1`); `src/ontology/registry.py` (`metadata` + four Table mirrors + `Origin` / `VerificationState` / `Polarity` Literals + `Concept` / `ConceptLemma` / `PolarityClaim` / `InverseClaim` frozen Pydantic + `ConceptRegistry` reader with `get_by_lemma` / `get_polarity_claims` / `get_inverse_claims` / `is_prior_grounded` / `get_lemmas_for_concept` / `empty()`); `src/validation/validator.py` (`ValidationResult.grounding`, `_rule_13_registry_grounding` with `RULE13_PRIOR_GROUNDED` warning code, `validate(plan, capability_registry, concept_registry=None)` signature; rule 13 is additive-not-blocking per DEC-049); `scripts/db/seed_registry.py` (single `engine.begin()` transaction, two-factor `--truncate` gate, `INSERT ... ON CONFLICT DO NOTHING`, every row written with explicit `origin='curated'`, `verification_state='unverified'`, `confidence=NULL`); `data/seeds/registry/{concepts,concept_lemmas,polarity_claims,inverse_claims}.csv` | `tests/unit/test_ontology_registry.py` (27 tests across 5 classes incl. JSON round-trip, frozen-mutation rejection, Literal validation, table-mirror parity); `tests/unit/test_rule_13_registry_grounding.py` (10 tests using `_StubRegistry(ConceptRegistry)` with `engine=None` for no-DB unit coverage; back-compat / unverified / verified / mixed / inverse / non-concept-ignored); `tests/integration/test_apply_schemas.py` (12 tests incl. CHECK-constraint IntegrityError raises for invalid origin / verification_state / polarity / negative evidence_count / out-of-range confidence); `tests/integration/test_concept_registry_reader.py` (14 tests incl. `get_lemmas_for_concept` extension); `tests/integration/test_concept_registry_seed.py` (10 tests including the **Slice C Track 1 exit gate**: `SELECT DISTINCT verification_state FROM <each table>` returns exactly `{'unverified'}` — corpus-is-ground-truth invariant) | DEC-024, DEC-025 (boundary held), DEC-007 (grounding axis), DEC-015 (additive-warning rule), DEC-049 (umbrella for the four invariants), DEC-052 (DEC-025 boundary preserved via `src/engine/_schema.py` once executor lands) |
| REQ:08.polarity-claims-table | Evidence-bearing relational table for polarity claims (one row per concept-pole pair) with origin, evidence_count, verification_state, confidence | `data/schemas/02_concept_registry.sql` (`polarity_claims` with `UNIQUE (concept_id, polarity)` + CHECK constraints on `polarity IN ('+', '-', '±')`, `evidence_count >= 0`, `confidence IS NULL OR 0..1`); `src/ontology/registry.py` (`polarity_claims_table` Core mirror + `PolarityClaim` Pydantic) | `tests/integration/test_apply_schemas.py::test_polarity_claims_unique_per_concept_pole`, `::test_polarity_check_rejects_invalid_value`, `::test_evidence_count_check_rejects_negative_value`; `tests/integration/test_concept_registry_seed.py` (19 polarity rows seeded, all `verification_state='unverified'`); `tests/integration/test_concept_registry_reader.py::test_get_polarity_claims_*` | DEC-024, DEC-049 |
| REQ:08.inverse-claims-table | Evidence-bearing relational table for inverse claims (asymmetric pairs) with origin, evidence_count, verification_state, confidence | `data/schemas/02_concept_registry.sql` (`inverse_claims` with `UNIQUE (concept_id, inverse_concept_id)` + `CHECK (concept_id <> inverse_concept_id)` + value-domain CHECKs); `src/ontology/registry.py` (`inverse_claims_table` Core mirror + `InverseClaim` Pydantic) | `tests/integration/test_apply_schemas.py::test_inverse_claims_self_inverse_check_constraint`; `tests/integration/test_concept_registry_seed.py` (7 inverse-pair rows seeded); `tests/integration/test_concept_registry_reader.py::test_get_inverse_claims_*` | DEC-024, DEC-049 |
| REQ:09.pattern-engine-slice-l | **Slice L extension** of REQ:09.pattern-engine (existing row preserved below). Adds cross-verse window execution via `tokens.global_position`, COOCCURRENCE operator execution with abs-distance semantics (DEC-097), and `MatchCandidate.proximity: ProximityInfo | None` (DEC-095). `_extend_chains_window_step` (forward range for PRECEDENCE, symmetric for COOCCURRENCE — L-CLOSE-002); `_build_proximity_infos` batched fetch + top-20 intervening-lemma cap; `_gap_satisfied_unordered` for `~` arithmetic; chain-id filter blocks token reuse in `A ~ B ~ A` (L-CLOSE-003); `span_tokens` computed from min/max matched gp (L-CLOSE-005). | `src/engine/executor.py` (`_extend_chains_window_step`, `_build_proximity_infos`, `_step_pair_satisfied`, `_gap_satisfied_unordered`); `src/engine/models.py` (`ProximityInfo`, `MatchCandidate.proximity`) | `tests/unit/test_executor.py::TestWindowExecution`, `::TestProximityInfoPopulation`, `::TestCodexClosures`, `::TestGapSatisfiedUnordered`; `tests/unit/test_models.py::TestProximityInfo`, `::TestMatchCandidateProximityField` | DEC-094, DEC-095, DEC-097 |
| REQ:09.pattern-engine | SQL-based sequence search executor: `execute(plan, scope, engine, concept_registry=None) -> list[MatchCandidate]`; resolves LEMMA + CONCEPT NodeRefs, applies PRECEDENCE operators with optional GapConstraint, honors ScopeConstraint{corpus, language, books, unit=verse}; raises `UnsupportedPlanShape` on out-of-MVP-contract plans (negated nodes, morph_filters, malformed operator counts, AlternativeExpr/OptionalExpr/GroupExpr/InverseExpr at sequence level, non-LEMMA/CONCEPT NodeTypes, non-PRECEDENCE operators, non-verse scope units); raises `RegistryRequired` when a CONCEPT step lacks a registry; raises `ConceptNotMapped` when a CONCEPT resolves to `[]` lemmas | `src/engine/executor.py` (`execute`, `validate_plan_shape`, `resolve_step_lemmas`, `build_scope_where` — public per DEC-059 so `src/retrieval/` can reuse them; `_extend_chains_one_step`, `_gap_satisfied`, `_row_to_matched_token`, `_format_reference` remain private); `src/engine/models.py` (`MatchedToken`, `StepMatch`, `MatchCandidate`, `UnsupportedPlanShape`, `RegistryRequired`, `ConceptNotMapped`); `src/engine/_schema.py` (private `_metadata` + `tokens_table` mirror — DEC-052 boundary fix); `src/ontology/book_codes.py` (abbrev↔BB↔display map; closes Bucket 3); `scripts/query.py` (parse → validate → retrieve → render CLI; exit 0/1/2/3 taxonomy mirroring ingest_corpus.py; calls `retrieve(contextualize=True)` per DEC-056) | `tests/unit/test_executor.py` (14 tests, no DB; covers every UnsupportedPlanShape / RegistryRequired / ConceptNotMapped raise path + scope-filter-on-every-step compile assertions); `tests/unit/test_book_codes.py` (7 tests incl. silent-miss guard `test_canonical_07_examples_all_resolve`); `tests/integration/test_executor.py` (7 tests incl. Slice C exit gate `test_full_corpus_faith_hope_love_in_1cor_13_13`); `tests/integration/test_query_cli.py` (9 tests incl. supported / partial / unsupported / no-matches / password-redaction / parse-error / unknown-concept exit-3 paths + 2 Slice D contextualization-rendering tests) | DEC-014 (engine match-type support), DEC-007 (results distinguish match types), DEC-026 (BB-digit storage), DEC-024 (no silent coercion), DEC-050 (MVP contract: raise on out-of-shape), DEC-051 (Track 2 contextualization deferred to Slice D), DEC-052 (DEC-025 boundary via `src/engine/_schema.py`), DEC-053 (book_codes.py placement), DEC-059 (helpers promoted to public for retrieval-layer reuse) |
| REQ:09.retrieval-pipeline | Multi-stage retrieval orchestrator (canonical-09 §6): `retrieve(plan, scope, engine, *, contextualize=False, registry=None) -> RetrievalResult`. MVP is a thin wrapper around the pattern engine plus an optional contextualization stage; the interface exists so semantic retrieval can be added later as additional stages. Engine-layer default-off / API-CLI default-on per DEC-056. | `src/retrieval/retrieve.py` (`retrieve()`); `src/engine/models.py` (`RetrievalResult` frozen Pydantic with `candidates`, `stages_used`, `contextualization`); `scripts/query.py` (UI-layer consumer; passes `contextualize=True`) | `tests/unit/test_retrieve.py` (5 tests: default-off, contextualize=True envelope, exception propagation, empty-candidate case, registry pass-through); `tests/integration/test_contextualization.py` (2 retrieve()-pipeline tests covering default-off + contextualize=True against the full NT + seeded registry); `tests/integration/test_query_cli.py` (CLI consumes RetrievalResult.contextualization via the slice exit gate) | DEC-024 (corpus-is-ground-truth), DEC-054 (REQ marker), DEC-055 (code home `src/retrieval/`), DEC-056 (default split), DEC-059 (executor helpers promoted) |
| REQ:09.contextualization | Result-set calibration envelope (canonical-09 §8): per-node baselines, alternative-ordering counts, and a reserved null-distribution slot. Hangs on `RetrievalResult.contextualization` (and on `ExplainedResultSet.contextualization` once the explainer slice ships). Four invariants: per-node baselines, alt-ordering counts capped at `min(N!, 24)`, null-distribution slot reserved (always None in MVP per DEC-057), explainer must surface contextualization. | `src/retrieval/contextualization.py` (`compute_node_baselines`, `compute_alternative_orderings`, `contextualize`, private `_fallback_permutations` truncated at `_MAX_PERMUTATIONS=24`, `_format_sequence_label`); `src/retrieval/retrieve.py` (`retrieve(contextualize=True)` consumer — the only producer of populated envelopes today); `src/engine/models.py` (`NodeBaseline`, `AlternativeOrderingCount`, `NullDistribution`, `Contextualization`, `RetrievalResult.contextualization`; `NonNegativeInt` / `NonNegativeFloat` schema-enforced ge=0 invariants per Codex D-D1D2-002); `scripts/query.py::_print_contextualization` (UI-layer rendering: observed count, baselines, alt-orderings sorted by count desc with observed marked *, null-distribution "not computed in MVP") | `tests/unit/test_contextualization.py` (24 tests across baseline / alt-ordering / orchestrator / fallback-permutation / sequence-label classes — covers shape failures, scope language propagation, observed-vs-alternative marking, capped-flag correctness, N=30 boundary truncation); `tests/unit/test_models.py` (5 test classes covering NodeBaseline / AlternativeOrderingCount / NullDistribution / Contextualization / RetrievalResult — frozen, JSON round-trip, negative-value rejection); `tests/integration/test_contextualization.py` (7 tests incl. lemma=direct-COUNT, concept=sum-of-resolved-lemmas, three-step faith>hope>love structural shape, scope filter reduces count, alt-ordering identity matches execute(), retrieve() default-off, retrieve() contextualize=True full-envelope); `tests/integration/test_query_cli.py::test_cli_renders_contextualization_for_flagship_sequence` (**Slice D exit gate**: `faith > hope > love` produces a stdout block with Constituent baselines + Alternative orderings + Null distribution slot); `tests/integration/test_query_cli.py::test_cli_renders_no_match_with_contextualization` (zero-match case still renders the envelope) | DEC-024 (corpus-is-ground-truth — output-side companion), DEC-054 (REQ marker + 4 invariants), DEC-055 (code home), DEC-056 (default split), DEC-057 (null-distribution deferred), DEC-058 (permutation cap policy), DEC-059 (helpers promoted) |
| REQ:04.matching-rules | Node-type → corpus field resolution table (canonical-04 §matching-rules); MVP supports the LEMMA and CONCEPT rows; ROOT / DOMAIN / MORPH / WILDCARD / TOKEN raise `UnsupportedPlanShape` at executor entry | `src/engine/executor.py::resolve_step_lemmas` (public per DEC-059; LEMMA → `[node.value]`; CONCEPT → `concept_registry.get_lemmas_for_concept(name, language)`); `src/engine/executor.py::validate_plan_shape` (public per DEC-059; rejects ROOT/DOMAIN/MORPH/WILDCARD/TOKEN with `UnsupportedPlanShape`); also consumed by `src/retrieval/contextualization.py::compute_node_baselines` (same resolution rules apply at the calibration layer) | `tests/unit/test_executor.py::test_raises_unsupported_on_wildcard_node` (and the broader UnsupportedPlanShape coverage); `tests/integration/test_executor.py::test_concept_step_resolves_via_registry`, `::test_single_lemma_step_returns_matches`; `tests/unit/test_contextualization.py::test_lemma_baseline_returns_self_resolution`, `::test_concept_baseline_uses_registry_resolution`; `tests/integration/test_contextualization.py::test_concept_baseline_equals_sum_of_resolved_lemmas` | DEC-014, DEC-050, DEC-059 |
| REQ:09.result-explainer | Result builder + explainer (canonical-09 §9): `explain(result: RetrievalResult, plan: QueryPlan, validation: ValidationResult, *, llm_client: LLMClient \| None = None) -> ExplainedResultSet` produces prose grounded in actual corpus counts. Deterministic by default; LLM-paraphrased prose for conceptual matches is opt-in via the kw-only `llm_client` argument. `ExplainedResultSet` carries `query_shown`, optional `nl_source`, `validation_notes` (raw finding strings), `results: list[ExplainedResult]`, optional `contextualization`, and a required `summary` (≤ 5 lines). `ExplainedResult` carries `reference`, `text_display`, `match_type`, optional `score`, `explanation`. Five invariants (canonical §9 (a)–(e)): RetrievalResult-keyed signature; field-derived prose only (no fabrication, DEC-024 + DEC-081); deterministic default with opt-in LLM-augmentation for conceptual matches + airtight deterministic fallback (DEC-061 + DEC-090); validation_notes raw-string format; cap policy in prose layer (lemmas ≤ 5 with "+N more"; sequence labels ≤ 64 chars with ellipsis; LLM prose ≤ 300 chars defense-in-depth) — `_print_contextualization` stays unbounded by design. | `src/nlp/explainer.py` (`explain()` orchestrator + private helpers `_summary_prose`, `_per_candidate_prose`, `_per_candidate_prose_llm`, `_is_fallback_signal`, `_truncate_llm_prose`, `_format_baselines_phrase`, `_format_alt_orderings_phrase`, `_verse_list_clause`, `_truncate_lemmas`, `_truncate_sequence_label`, `_sequence_label_for_plan`, `_text_display_for_candidate`, `_format_validation_notes`, `_format_finding`); `src/nlp/prompts/explainer_prompt.py` (`EXPLAINER_SYSTEM_PROMPT` + `build_explainer_user_message` — Slice K); `src/engine/models.py` (`ExplainedResult`, `ExplainedResultSet` frozen Pydantic; `score: float \| None = None` per DEC-061); `scripts/query.py` (CLI integration; `--no-prose` flag; `_print_explanation` helper; CLI does NOT opt into the LLM env flag — deterministic prose only at the CLI surface); `src/app/orchestration.py` (`run_nl_query` reads `SPL_EXPLAINER_LLM` env var and threads `llm_client` through `_run_dsl_pipeline_with_optional_explainer_llm` to `explain()`; `run_dsl_query` stays explicitly LLM-free); **Slice G**: `src/app/routes/query.py` is the second consumer (HTTP) of deterministic prose; **Slice K**: `src/app/routes/nl.py` is the only HTTP consumer that may surface LLM-paraphrased prose, gated by `SPL_EXPLAINER_LLM=1` at the deployed environment | `tests/unit/test_models.py::TestExplainedResult` + `::TestExplainedResultSet` (10 tests); `tests/unit/test_explainer.py` (57 tests: 37 deterministic baseline + 9 `TestPerCandidateProseLLM` (Slice K Phase K.2: helper happy path, FALLBACK token, FALLBACK with punctuation variants, FALLBACK negative for real prose containing the word, LLMUnavailable + unexpected-Exception fallback w/ caplog WARNING + exc_info=True, output truncation, grounded-input round-trip) + 9 `TestExplainWithLLMClient` (Slice K Phase K.3: byte-identical envelope when no client injected, LLM paraphrase happens, one LLM call per conceptual, variant + exact NOT routed through LLM, summary/contextualization/validation_notes unaffected, fallback envelope identical to deterministic, exact-match symmetric coverage K-MID-003)); `tests/unit/test_explainer_prompt.py` (17 tests covering EXPLAINER_SYSTEM_PROMPT no-fabrication clauses + build_explainer_user_message grounded-fields shape); `tests/unit/test_app_orchestration.py::TestRunNLQueryWithExplainerLLMOptIn` (7 tests covering SPL_EXPLAINER_LLM unset/`1`/`true`/`0`/`false`/empty/typo paths); `tests/integration/test_query_cli.py::test_cli_renders_explanation_for_flagship_sequence` (**Slice F exit gate**); `tests/integration/test_query_cli.py::test_cli_no_prose_flag_suppresses_explanation`; `tests/integration/test_app_dsl_route.py::test_query_dsl_returns_flagship_sequence_envelope` (**Slice G exit gate**); `tests/integration/test_explainer_llm_prose_live.py` (**Slice K exit gate**: 5 live_llm tests — deterministic baseline byte-identity when env var off, LLM path differs when env var on, no-fabrication grounded-substring check (DEC-081), single-sentence ≤ 300-char cap, verse-reference verbatim in prose) | DEC-015 (AI explains/expands/critiques — honored via deterministic templating + Slice K's opt-in LLM-paraphrase); DEC-024 (corpus-is-ground-truth — every prose claim field-derived); DEC-056 (UI default-on — prose ON by default in CLI); DEC-061 (RetrievalResult signature + LLM deferred to Bucket 7); DEC-063 (response envelope composes ExplainedResultSet verbatim); DEC-067 (concrete LLMClient base — pattern reused); DEC-070 (LLMUnavailable semantics — fallback contract); DEC-081 (no fabrication — structural enforcement via grounded-only user message); DEC-090 (Bucket 7 closure: opt-in LLM paraphrase for conceptual prose) |
| REQ:09.api-gateway | API gateway component (canonical-09 §1): `src/app/` owns HTTP routing, request validation, JSON I/O, typed request/response schemas, dependency injection, and the exception→HTTP-status mapping table. Slice G shipped foundation + DSL route + health. Slice H added `POST /api/v1/query/nl`. **Slice I closes the §1 routes table by shipping the three remaining MVP routes** — `GET /api/v1/capabilities` (returns `CapabilityRegistry.mvp()` directly per DEC-075; engine-free, no DI), `GET /api/v1/concepts` (returns flat `ConceptsResponse{concepts: list[ConceptSummary]}` with embedded lemmas per DEC-076; optional `language` query param; not paginated at MVP scale per Bucket 9), `POST /api/v1/query/validate` (returns `QueryValidateResponse{query, validation}` with `validation.status` carrying the verdict per DEC-079; only 422 path is parse_error). All six MVP routes from canonical-09 §1 are now coded. | `src/app/main.py` (`create_app()` factory + lifespan + module-level `app`); `src/app/dependencies.py` (`get_engine` / `get_concept_registry` providers raising 503 when state is None); `src/app/orchestration.py` (`run_dsl_query()` pipeline helper + `ValidationUnsupported`); `src/app/schemas.py` (`QueryDSLRequest`, `QueryDSLResponse`, `ErrorResponse`); `src/app/routes/__init__.py` (package marker); `src/app/routes/health.py` (`GET /api/v1/health`); `src/app/routes/query.py` (`POST /api/v1/query/dsl` handler + exception→HTTP-status `try/except` chain); `src/app/routes/capabilities.py`, `src/app/routes/concepts.py`, `src/app/routes/validate.py` (Slice I MVP routes) | `tests/unit/test_app_schemas.py` (14 tests: request rejection of empty/missing dsl, frozen, JSON round-trip, null emission for ErrorResponse.details and nested nullable fields); `tests/unit/test_app_orchestration.py` (8 tests across parse-error, validator-rejects-inverse, retrieve-side exception propagation, happy-path with mocked retrieve, partial-validation path); `tests/unit/test_app_main.py` (8 tests: factory isolation, lifespan with unset DATABASE_URL, lifespan with mocked engine builder, dependency_overrides supersede state lookup, 503 fallback when state is unset and no override); `tests/unit/test_app_health.py` (4 tests: 200, status body, JSON content-type, no-DB invariant); `tests/unit/test_app_routes.py` (13 tests: request body validation, every error→status mapping, happy path 200, null emission, partial-validation 200 with findings, route registration); `tests/unit/test_app_routes_capabilities.py`, `tests/unit/test_app_routes_concepts.py`, `tests/unit/test_app_routes_validate.py` (Slice I unit tests); `tests/integration/test_app_dsl_route.py` (5 tests: live `/health` against the live app; **Slice G exit gate** `test_query_dsl_returns_flagship_sequence_envelope` — JSON envelope shape, baselines 483/84/259, 6 alt-orderings, `null_distribution: null`, summary ≤ 5 lines; plus three sad-path live-DB integration assertions for parse_error/validation_unsupported/concept_not_mapped); `tests/integration/test_app_endpoint_followup.py` (Slice I integration) | DEC-024 (corpus is ground truth — response surfaces structured findings + raw counts, not prose-only); DEC-025 (engine⊥ingestion boundary — `src/app/main.py` reuses CLI's `src.ingestion.db.get_engine` carve-out); DEC-052 (cross-layer-narrowness precedent); DEC-056 (engine-default-False / API-CLI-default-True for `contextualize`); DEC-062 (factory + lifespan + DI architecture); DEC-063 (response envelope); DEC-064 (HTTP status code mapping); DEC-065 (null-field emission policy / OQ #6 resolution); DEC-066 (health-only + endpoint deferrals); DEC-075 (capabilities response_model); DEC-076 (concepts flat shape); DEC-079 (validate status routing) |
| REQ:09.nl-to-dsl-slice-l | **Slice L extension** of REQ:09.nl-to-dsl (existing row preserved below). `TranslationResult` becomes a discriminated union of `TranslationSuccess` (prior shape) + `TranslationNeedsClarification(question, suggested_windows=[10,20,50], nl_source)` (DEC-096). System prompt teaches two response shapes (DSL: vs Clarification:). Route handler short-circuits the clarification variant to a 200 with `clarification` field set and the four pipeline fields absent. Cookbook gains Proximity Vocabulary section + `~` worked examples + window(N) examples; "Coming Soon" updated to remove `~`. | `src/nlp/translator.py` (`TranslationSuccess`, `TranslationNeedsClarification`, `TranslationResult` Annotated union, `_parse_output` two-branch); `src/nlp/prompts/system_prompt.py` (two-shape framing); `src/app/schemas.py` (`ClarificationPayload`, `QueryNLResponse` flat schema); `src/app/orchestration.py::run_nl_query` (clarification short-circuit); `docs/agent/dsl-cookbook.md` (Proximity Vocabulary section) | `tests/unit/test_translator.py::TestTranslateClarificationPath` (2 tests: clarification line returns variant; DSL: takes precedence over Clarification:); `tests/unit/test_app_routes_nl.py::TestHappyPath::test_clarification_path_returns_200_with_clarification`; `tests/unit/test_app_schemas.py::TestQueryNLResponseShape::test_clarification_shape` | DEC-096 |
| REQ:09.nl-to-dsl-slice-m | **Slice M extension** of REQ:09.nl-to-dsl (existing row preserved below). Adds caller-driven STATELESS multi-turn refinement on top of the Slice L clarification path (DEC-098). LLM seam gains an additive `complete_turns(system_prompt, turns)` (multi-message array) alongside the unchanged single-shot `complete()`; the static `SYSTEM_PROMPT` prefix stays cached on both seams (DEC-071 amended). `translate()` gains `prior_turns: list[Message] \| None` — empty → single-shot byte-identical, non-empty → `_build_turns` assembles `[user(orig+registry summaries), …prior verbatim…, user(nl_query)]` and calls `complete_turns`. `run_nl_query` converts app-schema `ConversationTurn`→nlp-layer `Message` at the boundary (src/nlp never imports src/app, DEC-052). Existing `/query/nl` route extended, no new route (DEC-099). New frozen `ConversationTurn` + `QueryNLRequest.prior_turns` (max_length=20) with a deterministic `@model_validator` enforcing conversation shape (starts user / alternates / ends assistant) + aggregate 16000-char cap → clean 422 (M-CLOSE-001/002); resource guards only, no semantic round cap (DEC-100, OQ-2). | `src/nlp/llm_client.py` (`Message`, `complete_turns`, shared `_UNAVAILABLE_ERRORS` + `_extract_text`); `src/nlp/translator.py` (`translate` prior_turns branch, `_build_turns`); `src/nlp/prompts/system_prompt.py` (minimal framing addition); `src/app/schemas.py` (`ConversationTurn`, `QueryNLRequest._validate_conversation_shape`); `src/app/orchestration.py::run_nl_query` (ConversationTurn→Message conversion); `src/app/routes/nl.py` (forwards body.prior_turns); `web/server/api/sp/query/nl.post.ts` (proxy zod passthrough); `docs/canonical/09_backend-service-boundaries.md` §2 | `tests/unit/test_llm_client.py::TestAnthropicCompleteTurns`, `::TestAnthropicCompleteTurnsErrorWrapping`, `::TestCachePrefixRegressionGuard`; `tests/unit/test_translator.py::TestTranslateMultiTurn`; `tests/unit/test_app_orchestration.py::TestRunNLQueryPriorTurns`; `tests/unit/test_app_schemas.py::TestQueryNLRequestConversationShape`; `tests/unit/test_app_nl_refinement.py::TestTwoRoundRefinementExitGate` (**Slice M exit gate**) + `test_round_two_can_clarify_again`; `tests/integration/test_app_nl_refinement_live_llm.py` (live_llm twin); `web/tests/server/nl.post.test.ts` | DEC-098 (stateless echo-back), DEC-099 (same route), DEC-100 (schema + shape validation + resource guards), DEC-101 (frontend deferred), DEC-071 (amended — single-shot default, cached prefix), DEC-052 (app↔nlp boundary), DEC-070 (4xx propagates raw — why the unguarded path was a 500) |
| REQ:09.nl-to-dsl | NL→DSL translator (canonical-09 §2): single-shot LLM call returning a `TranslationResult{dsl, confidence, alternatives, explanation}` per the canonical interface. `LLMClient` is a concrete base class (DEC-067, not Protocol) with `.complete(system, user) -> str`; `AnthropicLLMClient` is the sole concrete child for MVP. System prompt assembled at module import time from `docs/agent/dsl-cookbook.md` plus a compile-only translator framing (DEC-071). Translator output extracted via line-anchored regex against a `DSL: <string>` line plus optional `Confidence:`, `Alternatives:`, `Explanation:` lines; missing/empty `DSL:` raises `NLCompileError`. Failure-mode wrapping in `AnthropicLLMClient.complete` is narrowed to availability + auth + 5xx server exceptions (DEC-070, H-H1H2-001) — 4xx code-bug exceptions propagate raw. No confidence-threshold gate (DEC-072: corpus-is-ground-truth charter). | `src/nlp/llm_client.py` (`LLMClient`, `AnthropicLLMClient`, `LLMUnavailable`, `build_anthropic_client_from_env`); `src/nlp/translator.py` (`translate`, `TranslationContext`, `TranslationResult`, `NLCompileError`, 5 line-anchored regex helpers); `src/nlp/prompts/system_prompt.py` (`SYSTEM_PROMPT` module constant + `build_system_prompt`); `src/app/schemas.py` (`QueryNLRequest`, `QueryNLResponse(QueryDSLResponse)`, `TranslationMetadata`); `src/app/orchestration.py::run_nl_query` (composes `translate` + `run_dsl_query`); `src/app/dependencies.py` (`get_llm_client`, `get_translation_context`); `src/app/main.py` (lifespan reads `ANTHROPIC_API_KEY`, builds `LLMClient` + `TranslationContext` independently from DB); `src/app/routes/nl.py` (`POST /api/v1/query/nl` handler + 8-branch exception mapping per DEC-070). | `tests/unit/test_llm_client.py` (17 tests: base class abstract behavior, construction with/without API key, happy-path + text-block filtering, narrowed error wrapping for availability + auth + 5xx + permission, parameterized 4xx-propagate-raw set, build-from-env behavior); `tests/unit/test_translator.py` (19 tests: system prompt assembly + cookbook embed + framing, output parsing happy/missing/empty/malformed/asterisk-bullets, default-value behavior, LLMUnavailable propagation, frozen invariants); `tests/unit/test_app_orchestration.py::TestRunNLQueryHappyPath` + `::TestRunNLQueryTranslatorFailures` + `::TestRunNLQueryDownstreamFailures` (6 tests); `tests/unit/test_app_schemas.py::TestQueryNLRequest` + `::TestTranslationMetadata` + `::TestQueryNLResponseInheritance` (6 tests covering construction, frozen, subclass relation); `tests/unit/test_app_routes_nl.py` (11 tests across happy-path, request validation, translator-side errors, every downstream pipeline error, provider-503-when-state-missing); `tests/unit/test_app_main.py::TestLifespanWithAnthropicApiKey` (2 tests: independent ANTHROPIC_API_KEY branch + DB-set-but-LLM-unset degradation); `tests/integration/test_app_nl_route_live_llm.py` (**Slice H exit gate** + ambiguity-surfacing test; gated by `live_llm` + `integration` markers, requires `DATABASE_URL` + `ANTHROPIC_API_KEY` to run). | DEC-003 (NL compiles to DSL, never bypass), DEC-006 (system says when it cannot do something), DEC-024 (corpus is ground truth — no confidence-threshold gating), DEC-061 (deterministic-first; LLM-prose-explainer is Bucket 7), DEC-067 (concrete LLMClient, not Protocol), DEC-068 (`live_llm` pytest marker), DEC-069 (`QueryNLResponse(QueryDSLResponse)` subclass envelope), DEC-070 (HTTP error mapping for translator failures + narrowed wrapping per H-H1H2-001), DEC-071 (system prompt assembled from cookbook at import time), DEC-072 (no confidence-threshold gate), DEC-073 (alternatives surfaced not auto-retried), DEC-074 (lifespan-scoped LLMClient + TranslationContext via `get_llm_client` / `get_translation_context` providers) |
| REQ:09.request-lifecycle | The 11-step ordered pipeline from "request hits FastAPI" through "response leaves FastAPI" (canonical-09 §request-lifecycle). For `POST /api/v1/query/dsl` step 3 (NL→DSL) is skipped per the spec note. **Slice H closes the lifecycle on the NL path**: step 3 (NL→DSL translator calls LLM, returns DSL string + explanation) is now coded via `src/app/orchestration.py::run_nl_query`. Slice G + H together realize all canonical lifecycle steps that apply to MVP (step 9 — scoring — remains a no-op per REQ:09.scoring-ranking gap). | `src/app/routes/query.py::post_query_dsl` (steps 1–2 = FastAPI body validation; step 4 via `src/engine/parser.py::parse`; step 5 via `src/validation/validator.py::validate`; step 5a/5b/5c branch via `ValidationUnsupported` + partial-status pass-through in `run_dsl_query`; steps 6–8 via `src/retrieval/retrieve.py::retrieve(contextualize=True)`; step 10 via `src/nlp/explainer.py::explain`; step 11 returns the `QueryDSLResponse` JSON envelope); `src/app/orchestration.py::run_dsl_query` (the in-process orchestrator that threads the executable plan through retrieve + explain) | `tests/unit/test_app_orchestration.py` (mocks each pipeline stage to assert step ordering and exception propagation match `scripts/query.py::main`); `tests/unit/test_app_routes.py::TestErrorMapping` (steps 4 / 5 / 6 each exercised via their respective exception paths); `tests/integration/test_app_dsl_route.py::test_query_dsl_returns_flagship_sequence_envelope` (full end-to-end on the real corpus: all 8 lifecycle steps that apply to `/dsl` execute and surface in the JSON envelope; step 9 — scoring — is still a no-op per REQ:09.scoring-ranking gap) | DEC-024 (corpus is ground truth — the full lifecycle's transparency invariants now apply to HTTP consumers too); DEC-061 (RetrievalResult signature for explainer); DEC-062 (factory + lifespan + DI architecture); DEC-063 (response envelope); DEC-064 (HTTP status mapping for the lifecycle's error responses) |
| _— Canonical 01 (Product Foundation) — added 2026-05-23 audit —_ | | | | |
| REQ:01.job-to-be-done | Umbrella requirement: NL → translate → validate → execute supported subset → explain. Realized by the full request-lifecycle (see REQ:09.request-lifecycle); this row is the canonical-01 entry-point for the product thesis. | `src/app/routes/nl.py` (NL→translate→validate→execute→explain front door); `src/nlp/translator.py` (step 1); `src/validation/validator.py` (step 2); `src/engine/executor.py` + `src/retrieval/retrieve.py` (step 3); `src/nlp/explainer.py` (step 4) | `tests/integration/test_app_nl_route_live_llm.py` (end-to-end Slice H exit gate exercising all four canonical-01 sub-steps with a live LLM); `tests/integration/test_app_dsl_route.py::test_query_dsl_returns_flagship_sequence_envelope` (the same four sub-steps for the DSL-direct surface, skipping translation) | DEC-001, DEC-003, DEC-006 |
| REQ:01.nl-compiles-to-dsl | Natural language must compile to DSL before reaching the engine; AI never bypasses the formal layer | `src/nlp/translator.py::translate` (single producer of DSL strings from NL); `src/app/orchestration.py::run_nl_query` (the only caller that wires NL→DSL→engine in HTTP); `src/nlp/prompts/system_prompt.py` (system prompt embeds the DSL cookbook so the LLM cannot improvise out-of-grammar shapes); `src/engine/parser.py::parse` (downstream gate — translator output must pass through this parser) | `tests/unit/test_translator.py` (19 tests asserting `DSL:` line is load-bearing, malformed → `NLCompileError`, no NL→engine bypass path); `tests/unit/test_app_orchestration.py::TestRunNLQueryHappyPath` (translator output threads through DSL pipeline); `tests/integration/test_app_nl_route_live_llm.py` (live exit-gate proves the NL surface never reaches the engine without the parser step) | DEC-003, DEC-071 |
| REQ:01.capability-boundary | System must say "I can't do that yet" rather than improvise unsupported analysis; partial reduction is explicit; validator findings are surfaced verbatim | `src/validation/validator.py` (rules 1–13, status `supported`/`partial`/`unsupported`, `_reduce_*` partial-plan helpers); `src/engine/executor.py` (`UnsupportedPlanShape`/`RegistryRequired`/`ConceptNotMapped` raised rather than silently coerced); `src/app/routes/query.py`, `src/app/routes/nl.py`, `src/app/routes/validate.py` (each maps validator/executor refusal to a structured 422 response with `error` code + `details`); `src/nlp/explainer.py::_format_validation_notes` (raw-string findings in user-facing prose) | `tests/unit/test_validator.py::TestRule6Inverse`, `::TestRule7Expansion`, `::TestRule8CompoundNodes`, `::TestRule10Scope`, `::TestRule11SequenceLength`, `::TestPartialReduction` (each capability gap produces a finding); `tests/unit/test_app_routes.py::TestErrorMapping`, `tests/unit/test_app_routes_validate.py::TestValidateUnsupportedReturnsTwoHundredNotFourTwentyTwo`; `tests/integration/test_query_cli.py` (CLI exit code 1 on unsupported / unsupported-shape paths) | DEC-006, DEC-050, DEC-064, DEC-079 |
| REQ:01.transparent-evidence | The product shows the generated query, explains why results matched, and labels exact / variant / conceptual / inverse / intertwined matches distinctly | `src/engine/models.py::MatchCandidate.match_type` (`Literal["exact", "variant", "conceptual"]` per MVP scope); `src/nlp/explainer.py::explain` (returns `query_shown`, per-candidate `explanation`, `validation_notes`, `contextualization`); `src/app/schemas.py::QueryDSLResponse` (echoes the executed DSL back as `query`); `scripts/query.py` (CLI surfaces the generated DSL, match-type labels, and contextualization block to the user) | `tests/unit/test_models.py::TestMatchCandidate::test_match_type_literal_validation` + `::test_match_type_conceptual_accepted`; `tests/unit/test_explainer.py` (37+ deterministic-prose tests asserting fields are surfaced not invented); `tests/integration/test_query_cli.py::test_cli_renders_explanation_for_flagship_sequence` (slice-F exit gate); `tests/integration/test_app_dsl_route.py::test_query_dsl_returns_flagship_sequence_envelope` (HTTP envelope carries `query`, `validation`, `result`, `explanation` verbatim) | DEC-007, DEC-024, DEC-063, DEC-081 |
| _— Canonical 02 (Query Language Draft) — added 2026-05-23 audit —_ | | | | |
| REQ:02.core-model | DSL represents sequence graphs (nodes, order, alternatives, optional, gaps, scope, polarity, expansion, ranking) — not bag-of-words | `src/engine/models.py` (`QueryPlan`, `SequenceExpr`, `StepExpr` union, `OrderOperator`, `AlternativeExpr`, `OptionalExpr`, `GapConstraint`, `ScopeConstraint`, `MatchMode`, `ExpansionDirective`, `RankingPrefs` — every concept in §13 has a typed AST node); `src/engine/parser.py` (compiles DSL surface into the AST union) | `tests/unit/test_models.py` (every AST class has construct + frozen + dump tests; `TestJsonRoundTrips` exercises canonical-05 §13 example shapes); `tests/unit/test_parser.py::TestDoc07Examples` (8 canonical-07 examples round-trip into the AST) | DEC-008, DEC-009 |
| REQ:02.typed-nodes | `lemma:`, `concept:`, `root:`, `morph:`, `domain:` typed-node syntax | `src/engine/parser.py::_parse_typed_value` (handles `<type>:<value>` prefixes for the 5 types); `src/engine/models.py::NodeType` (StrEnum: TOKEN/LEMMA/ROOT/CONCEPT/DOMAIN/MORPH/WILDCARD) | `tests/unit/test_parser.py::TestTokenize::test_typed_node`, `TestParseNodeRef::test_typed_node`; `tests/unit/test_models.py::TestEnums::test_node_type_values` | DEC-009, DEC-010 |
| REQ:02.operators | `>` precedence, `>>` adjacency, `~` cooccurrence, `\|` alternatives, `[]` optional, `!` exclusion, `()` grouping | `src/engine/parser.py` (tokenizer emits `ARROW`/`ADJACENCY`/`TILDE`/`PIPE`/`LBRACKET`/`RBRACKET`/`BANG`/`LPAREN`/`RPAREN`; parser maps `>`→precedence, `>>`→adjacency, `~`→cooccurrence); `src/engine/models.py::OperatorType` (PRECEDENCE/ADJACENCY/COOCCURRENCE) | `tests/unit/test_parser.py::TestTokenize::test_adjacency_operator`, `::test_cooccurrence_operator`, `::test_alternatives`, `::test_optional`, `::test_bang`, `::test_arrow`; `TestParseSequence::test_adjacency`, `::test_cooccurrence`; `TestParseAlternative`; `TestParseOptional::test_optional_node`; `TestParseNegation::test_negated_node` | DEC-009 |
| REQ:02.constraints | `>{0,5}` gap, `within:`, `lang:`, `corpus:`, `book:`, `mode:exact\|variant\|conceptual\|hybrid` | `src/engine/parser.py` (`_parse_gap_constraint`, `_parse_directive` handles `within:`/`lang:`/`corpus:`/`book:`/`mode:`); `src/engine/models.py` (`GapConstraint`, `ScopeConstraint`, `MatchMode`) | `tests/unit/test_parser.py::TestTokenize::test_gap_constraint`; `TestParseSequence::test_with_gap`; `TestParseDirectives::test_scope_within`, `::test_scope_lang`, `::test_scope_corpus`, `::test_scope_book_list`, `::test_multiple_directives`, `::test_mode_directive` | DEC-009, DEC-010 |
| REQ:02.polarity-syntax | `+`, `-`, `±`, `inverse(...)` polarity surface | `src/engine/parser.py::_parse_polarity` (handles `+`/`-`/`±` prefix); `_distribute_polarity` (LPAREN distribution to NodeRef leaves); `_parse_inverse_function` (compiles `inverse(...)` into `InverseExpr`); `src/engine/models.py` (`NodeRef.polarity` `Literal["+", "-", "±"] \| None`, `InverseExpr`) | `tests/unit/test_parser.py::TestTokenize::test_polarity_plus`, `::test_polarity_minus`, `::test_polarity_plusminus`; `TestParseNodeRef::test_polarity_*`; `TestParseAlternative::test_polarity_distributes_to_alternative_options`; `TestParseInverse::test_inverse_wrapper`; `tests/unit/test_models.py::TestNodeRef::test_with_polarity` | DEC-009, DEC-011, DEC-031 |
| REQ:02.expansion-syntax | `=> forward:N`, `=> backward:N`, `=> expand:N`, `supersequence(...)` | `src/engine/parser.py::_parse_directive` (handles `=> forward:`/`=> backward:`/`=> expand:`); `src/engine/models.py::ExpansionDirective` + `ExpansionDirection` enum. **Note**: parser builds the AST node, but validator rule 7 rejects expansion under MVP capability registry (`expansion_support=False`) per partial-reduction; `supersequence(...)` is not yet parsed. | `tests/unit/test_parser.py::TestParseDirectives::test_expansion_forward`, `::test_expansion_expand`; `tests/unit/test_models.py::TestExpansionDirective`; `tests/unit/test_validator.py::TestRule7Expansion::test_expansion_unsupported` (partial-reduction strips it under v0.1 registry) | DEC-004, DEC-009, partial under v0.1 capability registry |
| REQ:02.validation-behavior | Every query is checked by capability validator against node types, operators, scopes, polarity, modes, corpora/languages; outcomes: supported / partial / unsupported / developer-mode-extension-suggestion | `src/validation/validator.py::validate` (sequential rules 1–13, status in `{supported, partial, unsupported}`); `src/validation/registry.py::CapabilityRegistry.mvp` (declarative manifest checked against); `src/app/routes/validate.py` (`POST /api/v1/query/validate` exposes the validator without running the engine) | `tests/unit/test_validator.py::TestRule1Version` through `TestRule12` + `TestPartialReduction` + `TestDoc07Validation` (every rule + every canonical-07 example); `tests/unit/test_app_routes_validate.py` (HTTP surface: all three statuses → 200, parse_error → 422) | DEC-006, DEC-064, DEC-079 |
| _— Canonical 03 (System Architecture Notes) — added 2026-05-23 audit —_ | | | | |
| REQ:03.text-layer | Canonical structured corpus with tokens, lemmas, morphology, and structural boundaries | `data/schemas/01_tokens.sql` (tokens table with surface_form / normalized_form / lemma / morph_code / pos / book / chapter / verse / position / global_position); `src/ingestion/corpus_parser.py` (MorphGNT → CorpusToken iterator); `src/ingestion/loader.py` (bulk insert); `src/ontology/book_codes.py` (BB↔abbrev↔display map) — same code home as REQ:08.token-schema | `tests/unit/test_corpus_parser.py`; `tests/unit/test_book_codes.py`; `tests/integration/test_corpus_ingest.py::test_full_corpus_smoke` (137,554-token Slice-B exit gate) | DEC-013, DEC-026, DEC-053 |
| REQ:03.pattern-engine | Deterministic search over ordered sequences, variants, gaps, polarity, extension logic | `src/engine/executor.py` (deterministic SQL-based sequence search); `src/engine/parser.py` (deterministic surface→AST); `src/validation/validator.py` (deterministic rule checks). Same code home as REQ:09.pattern-engine. | `tests/unit/test_executor.py` (14 deterministic shape tests, no DB); `tests/integration/test_executor.py` (7 deterministic search tests against the live corpus); `tests/integration/test_query_cli.py` (deterministic CLI exit-code taxonomy) | DEC-014, DEC-050 |
| REQ:03.capability-validator | First-class system component (not a prompt) checking generated queries against current DSL + engine support | `src/validation/validator.py` (the component); `src/validation/registry.py::CapabilityRegistry` (the manifest); `src/app/routes/validate.py` (HTTP surface). Same code home as REQ:09.capability-validator. | `tests/unit/test_validator.py`; `tests/unit/test_app_routes_validate.py` | DEC-006 |
| REQ:03.retrieval-strategy | Multi-stage retrieval (symbolic / lexical / semantic / rerank / LLM explanation). MVP: stage 1 (symbolic) is the only stage; the orchestrator interface exists so later stages can be added without an API break. | `src/retrieval/retrieve.py::retrieve` (the orchestrator with `stages_used` field on `RetrievalResult`); `src/retrieval/contextualization.py` (stage-adjacent calibration). Lexical / semantic / rerank stages **not yet implemented** — interface is the contract slot. | `tests/unit/test_retrieve.py::test_stages_used_carries_executor` (asserts the stages_used field is populated by the engine stage); `tests/integration/test_contextualization.py` | DEC-017, DEC-054, DEC-055 — lexical/semantic stages deferred to post-MVP scoring/agent slices |
| REQ:03.scoring-philosophy | Ranking should be transparent and weighted across lexical / morphology / semantic / polarity / rarity / contextual-coherence factors | _Not yet implemented._ `src/scoring/` exists as a package marker (`__init__.py` only). `src/engine/models.py::RankingFactor` and `::RankingPrefs` define the contract slot; `ExplainedResult.score` is `None` per DEC-061 until the scoring slice lands. | _No tests beyond `tests/unit/test_models.py::TestRankingPrefs` (slot exists) — full coverage lands with the scoring slice._ | DEC-019, DEC-061 (deferred to post-MVP scoring slice — Bucket equivalent) |
| _— Canonical 04 (Node Ontology) — added 2026-05-23 audit —_ | | | | |
| REQ:04.node-lemma | Lemma node type: dictionary headword in a specific language; matches all tokens whose lemma matches the form | `src/engine/models.py::NodeType.LEMMA`; `src/engine/parser.py::_parse_typed_value` (LEMMA branch); `src/engine/executor.py::resolve_step_lemmas` (LEMMA → `[node.value]`); `src/engine/executor.py::validate_plan_shape` (accepts LEMMA at sequence level) | `tests/unit/test_parser.py::TestParseNodeRef::test_typed_node`; `tests/unit/test_executor.py` (LEMMA happy-path resolution); `tests/integration/test_executor.py::test_single_lemma_step_returns_matches` | DEC-014, DEC-050 |
| REQ:04.node-root | Root node type: consonantal/morphological root underlying multiple lemmas. **v0.2+ — not in MVP.** | `src/engine/models.py::NodeType.ROOT` (enum value exists); `src/engine/parser.py::_parse_typed_value` (ROOT branch parses surface syntax). **Engine rejects**: `src/engine/executor.py::validate_plan_shape` raises `UnsupportedPlanShape` for ROOT; `src/validation/validator.py` rule 2 emits `UNSUPPORTED_NODE_TYPE` under v0.1 capability registry. | `tests/unit/test_parser.py::TestDoc07Examples::test_example_7_root_nodes` (parses, validator rejects); `tests/unit/test_validator.py::TestRule2NodeTypes::test_root_unsupported`; `tests/unit/test_executor.py` (executor refusal coverage) | DEC-009 (typed nodes), DEC-050 (raise on out-of-shape) — implementation deferred to v0.2 |
| REQ:04.node-concept | Concept node type: language-independent semantic concept realized by multiple lemmas across languages | `src/engine/models.py::NodeType.CONCEPT`; `src/engine/parser.py::_parse_typed_value` (CONCEPT branch); `src/ontology/registry.py::ConceptRegistry.get_lemmas_for_concept` (resolution layer); `src/engine/executor.py::resolve_step_lemmas` (CONCEPT path); `src/retrieval/contextualization.py::compute_node_baselines` (CONCEPT baselines via same resolution) | `tests/unit/test_parser.py::TestParseNodeRef::test_typed_node`; `tests/integration/test_concept_registry_reader.py::test_get_lemmas_for_concept_*`; `tests/integration/test_executor.py::test_concept_step_resolves_via_registry`; `tests/integration/test_contextualization.py::test_concept_baseline_equals_sum_of_resolved_lemmas` | DEC-014, DEC-024, DEC-050 |
| REQ:04.node-domain | Domain node type: broader semantic field grouping multiple concepts. **v0.2+ — not in MVP.** | `src/engine/models.py::NodeType.DOMAIN`; `src/engine/parser.py::_parse_typed_value` (DOMAIN branch parses surface syntax). **Engine rejects**: validator rule 2 emits `UNSUPPORTED_NODE_TYPE` under v0.1; executor raises `UnsupportedPlanShape`. | `tests/unit/test_validator.py::TestRule2NodeTypes::test_domain_unsupported`; `tests/unit/test_executor.py` (executor refusal coverage) | DEC-009, DEC-050 — implementation deferred to v0.2 |
| REQ:04.node-morph | Morphology node type: morphological feature/category; combinable as compound morph filter | `src/engine/models.py::NodeType.MORPH`, `MorphFilter`; `src/engine/parser.py::_parse_typed_value` (MORPH branch), `_lookahead_is_morph_compound` (compound `+morph:` chaining) | `tests/unit/test_parser.py::TestTokenize::test_typed_node` (morph cases); `tests/unit/test_models.py::TestMorphFilter`; `tests/unit/test_validator.py::TestRule8CompoundNodes::test_compound_unsupported` (validator rejects under v0.1) | DEC-009 — standalone MORPH at sequence level rejected by executor under v0.1, MORPH-as-compound-filter accepted at parser level but rejected by validator rule 8 |
| REQ:04.node-token | Token node type: exact surface form match | `src/engine/models.py::NodeType.TOKEN`; `src/engine/parser.py::_parse_typed_value` (TOKEN branch). **Engine rejects** at sequence level (executor focuses on LEMMA/CONCEPT in MVP) but parser builds the AST node. | `tests/unit/test_parser.py::TestParseNodeRef::test_typed_node` (TOKEN parse path) | DEC-009, DEC-050 — surface parse supported; executor MVP deliberately scopes to LEMMA + CONCEPT |
| REQ:04.node-wildcard | Wildcard node type: `*` matches any single token position | `src/engine/models.py::NodeType.WILDCARD`; `src/engine/parser.py` (tokenizer emits `*` as WORD; parser routes to WILDCARD branch) | `tests/unit/test_parser.py::TestTokenize::test_wildcard_star`, `TestParseNodeRef::test_wildcard`, `TestParseSequence::test_wildcard_in_sequence` | DEC-033 |
| REQ:04.node-hierarchy | Node-type specificity hierarchy (token → lemma → root/concept → domain; morph + wildcard orthogonal) | Encoded across the engine: `src/engine/models.py::NodeType` (all 7 enum values reflect the hierarchy); `src/ontology/registry.py` (`concept_lemmas` realizes concept→lemma; `polarity_claims`/`inverse_claims` realize concept relationships); resolution rules in `src/engine/executor.py::resolve_step_lemmas` (LEMMA = direct; CONCEPT = via registry — same shape as canonical-04 matching-rules table) | `tests/unit/test_models.py::TestEnums::test_node_type_values`; `tests/unit/test_ontology_registry.py` (Literal validation enforces the hierarchy at runtime) | DEC-014 — hierarchy is structural (no separate runtime check beyond enum + matching-rules) |
| REQ:04.compound-nodes | Compound nodes: `lemma:pistis+morph:NOUN`, `concept:faith+morph:VERB` — content + morph-filter conjunction | `src/engine/parser.py` (`_lookahead_is_morph_compound`, while-loop accumulating `MorphFilter`s onto a NodeRef); `src/engine/models.py::NodeRef.morph_filters` (`list[MorphFilter]`) | `tests/unit/test_models.py::TestNodeRef::test_with_morph_filters`; `tests/unit/test_validator.py::TestRule8CompoundNodes::test_compound_unsupported` (validator rejects under v0.1 capability registry) | DEC-009 — parsed but rejected by validator under v0.1 `compound_node_support=False` |
| REQ:04.polarity | Per-node polarity marker: `+`, `-`, `±`, or unmarked; resolved through concept registry (default polarity + inverse + polarity-group membership) | `src/engine/models.py::NodeRef.polarity` (`Literal["+", "-", "±"] \| None`); `src/engine/parser.py::_parse_polarity` (surface parsing); `_distribute_polarity` (polarity-on-parens distribution to AlternativeExpr leaves); `src/ontology/registry.py::PolarityClaim`, `InverseClaim` (registry-side resolution data); `src/validation/validator.py::_rule_13_registry_grounding` (warns when polarity-marked nodes lack `corpus_observed`/`human_confirmed` claims) | `tests/unit/test_parser.py::TestTokenize::test_polarity_*`, `TestParseNodeRef::test_polarity_*`, `TestParseAlternative::test_polarity_distributes_to_alternative_options`; `tests/unit/test_models.py::TestNodeRef::test_with_polarity`; `tests/unit/test_rule_13_registry_grounding.py` (10 polarity-grounding tests) | DEC-005, DEC-011, DEC-024, DEC-031, DEC-049 |
| _— Canonical 05 (DSL AST) — added 2026-05-23 audit —_ | | | | |
| REQ:05.query-plan | `QueryPlan` top-level AST: version, source, sequence, scope, mode, expansion, ranking, metadata | `src/engine/models.py::QueryPlan` (frozen Pydantic v2 with all 8 fields per canonical §13); `src/engine/parser.py::parse` returns `QueryPlan` | `tests/unit/test_models.py::TestQueryPlan` (construct + dump-structure tests); `tests/unit/test_models.py::TestJsonRoundTrips` (all 5 canonical-05 examples); `tests/unit/test_parser.py::TestParseFull` (`test_default_mode_*`, `test_version`, `test_source_preserved`) | DEC-008, DEC-009 |
| REQ:05.sequence-expr | `SequenceExpr { steps, operators }` — ordered list of steps with operators between adjacent steps | `src/engine/models.py::SequenceExpr` (with `steps: list[StepExpr]` + `operators: list[OrderOperator]`, length invariant: operators.length = steps.length − 1) | `tests/unit/test_models.py::TestSequenceExpr::test_simple_sequence`; `tests/unit/test_parser.py::TestParseSequence` (simple_three_step / with_gap / adjacency / cooccurrence) | DEC-009 |
| REQ:05.step-expr | `StepExpr` = `NodeRef \| GroupExpr \| AlternativeExpr \| OptionalExpr` (discriminated union) | `src/engine/models.py` (StepExpr Annotated discriminated union per `_get_expr_type` + Tag, with `NodeRef`/`GroupExpr`/`AlternativeExpr`/`OptionalExpr` arms); `src/engine/parser.py::parse_step` | `tests/unit/test_models.py::TestSequenceExpr`; `tests/unit/test_parser.py::TestParseSequence`, `TestParseAlternative`, `TestParseOptional` | DEC-009 |
| REQ:05.group-expr | `GroupExpr { sequence, negated }` — parenthesized sub-expression treated as a single step | `src/engine/models.py::GroupExpr`; `src/engine/parser.py` (LPAREN handling in parse_step) | `tests/unit/test_parser.py::TestParseSequence` (paren grouping cases); `tests/unit/test_models.py` (GroupExpr round-trip in JSON-round-trip tests) | DEC-009 |
| REQ:05.optional-expr | `OptionalExpr { inner }` — step that may or may not be present | `src/engine/models.py::OptionalExpr`; `src/engine/parser.py` (LBRACKET handling in parse_step) | `tests/unit/test_models.py::TestOptionalExpr::test_construct`, `::test_json_round_trip`; `tests/unit/test_parser.py::TestParseOptional::test_optional_node`; `tests/unit/test_parser.py::TestDoc07Examples::test_example_4_alternatives_and_optional` | DEC-009 |
| REQ:05.order-operator | `OrderOperator { type, gap }` with type ∈ {precedence, adjacency, cooccurrence}; surface mapping `>`/`>>`/`~`. **Slice L activates COOCCURRENCE execution** (DEC-097) inside verse scope and inside `within:window(N)`; `~{m,n}` carries a step-level gap with abs-distance semantics. | `src/engine/models.py::OrderOperator`, `OperatorType` enum; `src/engine/parser.py` (surface→OperatorType mapping; `~` optionally consumes `{m,n}`); `src/engine/executor.py::_step_pair_satisfied` (PRECEDENCE → `_gap_satisfied` / COOCCURRENCE → `_gap_satisfied_unordered`) | `tests/unit/test_models.py::TestOrderOperator::test_precedence_no_gap`, `::test_precedence_with_gap`, `::test_dump`; `tests/unit/test_parser.py::TestParseSequence::test_with_gap`, `::test_adjacency`, `::test_cooccurrence`, `::test_cooccurrence_with_gap`; `tests/unit/test_executor.py::TestGapSatisfiedUnordered` (4 tests); `tests/unit/test_executor.py::TestCodexClosures::test_windowed_cooccurrence_uses_symmetric_range`, `::test_chain_rejects_already_used_token` | DEC-009, DEC-097 |
| REQ:05.gap-constraint | `GapConstraint { min, max }`; DSL `>{0,5}` → `{min:0, max:5}`. Slice L: `~{m,n}` uses the same shape; abs-distance arithmetic. **Composes with `within:window(N)`** (DEC-094) — both apply; `GAP_NARROWED_BY_WINDOW` warning surfaces when `gap.max > window.n`. | `src/engine/models.py::GapConstraint` (defaults `min=0`, `max=None` = unbounded); `src/engine/parser.py::parse_gap` (consumed by both `>` and `~`); executor `_gap_satisfied` + `_gap_satisfied_unordered` | `tests/unit/test_models.py::TestGapConstraint::test_defaults`, `::test_explicit`, `::test_frozen`; `tests/unit/test_parser.py::TestTokenize::test_gap_constraint`, `TestParseSequence::test_with_gap`, `::test_cooccurrence_with_gap`; `tests/unit/test_executor.py::TestGapSatisfiedUnordered`; `tests/unit/test_validator.py::TestRule10WindowConstraints::test_gap_narrowed_by_window_warning` | DEC-009, DEC-094, DEC-097 |
| REQ:05.scope-constraint | `ScopeConstraint { corpus, language, books, unit }` where `unit` is a **discriminated union of `ScopeUnitVerse` + `ScopeUnitWindow(n)`** (DEC-093, Slice L). `clause`/`sentence`/`pericope`/`chapter` now parse-error (previously inert StrEnum values). `within:window(N)` executes via `tokens.global_position` anchored at `chain[0]` (DEC-094); book boundary blocks, chapter boundary crossable. | `src/engine/models.py::ScopeConstraint`, `ScopeUnitVerse`, `ScopeUnitWindow`, `ScopeUnit` (Annotated discriminated union); `src/engine/parser.py::parse_directives` (within:verse / within:window(N) branches; window(0) parse rejection); `src/engine/executor.py::validate_plan_shape` (accepts both kinds); `src/engine/executor.py::_extend_chains_window_step`, `_build_proximity_infos` (window execution path) | `tests/unit/test_models.py::TestScopeUnitDiscriminatedUnion` (5 tests including round-trip of both kinds); `tests/unit/test_models.py::TestScopeConstraint`; `tests/unit/test_parser.py::TestParseDirectives::test_scope_within`, `::test_scope_within_window`, `::test_scope_within_window_rejects_zero`, `::test_scope_within_window_requires_parens`, `::test_scope_within_rejects_unsupported_unit`; `tests/unit/test_executor.py::TestWindowExecution` (3 tests); `tests/unit/test_executor.py::TestProximityInfoPopulation` (3 tests); `tests/unit/test_executor.py::TestCodexClosures` (3 closures) | DEC-009, DEC-050, DEC-093, DEC-094 |
| REQ:05.match-mode | `MatchMode` = `Literal["exact", "variant", "conceptual", "hybrid"]` | `src/engine/models.py::MatchMode` (module-level Literal alias); `src/engine/parser.py` (default mode inference + `mode:` directive parsing) | `tests/unit/test_parser.py::TestParseFull::test_default_mode_concept`, `::test_default_mode_exact`; `TestParseDirectives::test_mode_directive`; `tests/unit/test_validator.py::TestRule1Version` (mode validation indirectly via rule 9 inside `_rule_9_match_mode`) | DEC-009, DEC-010 |
| REQ:05.expansion-directive | `ExpansionDirective { direction, depth }`; DSL `=> forward:2`/`=> expand:2` mappings | `src/engine/models.py::ExpansionDirective`, `ExpansionDirection` enum; `src/engine/parser.py::_parse_directive` (expansion branch). **Validator rejects under v0.1** per canonical-06 rule 7. | `tests/unit/test_models.py::TestExpansionDirective`; `tests/unit/test_parser.py::TestParseDirectives::test_expansion_forward`, `::test_expansion_expand`; `tests/unit/test_validator.py::TestRule7Expansion::test_expansion_unsupported` | DEC-004, DEC-009 — partial under v0.1 (parser builds AST, validator strips) |
| REQ:05.ranking-prefs | `RankingPrefs { weights: Map<RankingFactor, float> }`; `RankingFactor` ∈ {lexical_alignment, morphology_alignment, semantic_overlap, polarity_fidelity, rarity, contextual_coherence} | `src/engine/models.py::RankingPrefs`, `RankingFactor` enum (all 6 factors). **Slot only**: no scoring component yet consumes it — see REQ:09.scoring-ranking. | `tests/unit/test_models.py::TestRankingPrefs::test_construct`, `::test_dump`; `tests/unit/test_models.py::TestEnums::test_ranking_factor_values` | DEC-019, DEC-061 — slot exists, consumer deferred to scoring slice |
| REQ:05.inverse-expr | `InverseExpr { inner: SequenceExpr }` — wrapper instructing engine to resolve to inverse-pole equivalents | `src/engine/models.py::InverseExpr`; `src/engine/parser.py::_parse_inverse_function` (compiles `inverse(...)` surface). **Validator rejects under v0.1** per canonical-06 rule 6 (`inverse_support=False`). | `tests/unit/test_models.py::TestInverseExpr::test_construct`; `tests/unit/test_parser.py::TestTokenize::test_inverse_function`, `TestParseInverse::test_inverse_wrapper`, `TestDoc07Examples::test_example_5_inverse`; `tests/unit/test_validator.py::TestRule6Inverse::test_inverse_unsupported` | DEC-005, DEC-009, DEC-011 — partial under v0.1 (parser builds AST, validator rejects) |
| REQ:05.serialization | AST must be JSON-serializable for logging, caching, debugging, comparison | All AST classes in `src/engine/models.py` are Pydantic v2 `BaseModel` with `model_dump_json()` / `model_validate_json()` round-trip. Frozen + JSON-schema-derived serialization across the union types via the `_get_expr_type` discriminator. | `tests/unit/test_models.py::TestJsonRoundTrips` (5 canonical-05 examples round-trip); `tests/unit/test_models.py::TestNodeRef::test_json_round_trip` and similar per-class round-trip tests (10+ classes covered) | DEC-009 |
| _— Canonical 06 (Capability Validator) — added 2026-05-23 audit —_ | | | | |
| REQ:06.validation-result | `ValidationResult { status, executablePlan, findings, engineVersion, suggestion, grounding }` | `src/validation/validator.py::ValidationResult` (frozen Pydantic with all canonical fields + `grounding` axis per DEC-007) | `tests/unit/test_validator.py::TestValidationResult::test_supported`; `tests/unit/test_rule_13_registry_grounding.py` (10 tests exercise the `grounding` field) | DEC-006, DEC-007, DEC-049 |
| REQ:06.validation-finding | `ValidationFinding { severity, code, path, message, remediation }` | `src/validation/validator.py::ValidationFinding` (frozen Pydantic with all 5 canonical fields) | Asserted across `tests/unit/test_validator.py` (every rule's test inspects `findings[].code`, `severity`, `path`); `tests/unit/test_rule_13_registry_grounding.py` (severity + code assertions) | DEC-006 |
| REQ:06.capability-registry | `CapabilityRegistry` — declarative manifest of supported nodes / operators / modes / scopes / flags + `mvp()` factory. **Slice L adds `scope_units: list[str]`** (["verse", "window"]) and **`window_max_tokens: int`** (50) and adds `"cooccurrence"` to `operators`. | `src/validation/registry.py::CapabilityRegistry` (frozen Pydantic v2; `CapabilityRegistry.mvp()` returns the canonical v0.1 manifest from canonical-06 §95–112 — now including scope_units + window_max_tokens + cooccurrence); `src/app/routes/capabilities.py` exposes it as `GET /api/v1/capabilities` per DEC-075 | `tests/unit/test_validator.py::TestCapabilityRegistry::test_mvp_version`, `::test_mvp_node_types`, `::test_mvp_operators` (asserts `cooccurrence` present), `::test_mvp_scope_units` (asserts scope_units + window_max_tokens), `::test_mvp_corpora`, `::test_mvp_flags`; `tests/unit/test_app_routes_capabilities.py::TestCapabilitiesRoute::test_body_contains_slice_l_proximity_fields` | DEC-006, DEC-075, DEC-097 |
| REQ:06.rule-1 | Rule 1: Version compatibility check | `src/validation/validator.py::_rule_1_version` (in `_RULES` chain) | `tests/unit/test_validator.py::TestRule1Version::test_compatible`, `::test_incompatible` | DEC-006 |
| REQ:06.rule-2 | Rule 2: Node type support — `UNSUPPORTED_NODE_TYPE` | `src/validation/validator.py::_rule_2_node_types` | `tests/unit/test_validator.py::TestRule2NodeTypes::test_supported_types`, `::test_root_unsupported`, `::test_domain_unsupported` | DEC-006 |
| REQ:06.rule-3 | Rule 3: Operator support — `UNSUPPORTED_OPERATOR` | `src/validation/validator.py::_rule_3_operators` | `tests/unit/test_validator.py::TestRule3Operators::test_precedence_supported`, `::test_cooccurrence_unsupported` | DEC-006 |
| REQ:06.rule-4 | Rule 4: Gap constraint support — `UNSUPPORTED_GAP_CONSTRAINT`, `GAP_EXCEEDS_MAX` | `src/validation/validator.py::_rule_4_gap_constraints` (gap support is True under v0.1; `max_gap=None` makes upper-bound trivially satisfied) | `tests/unit/test_validator.py` (gap-bearing inputs covered via TestRule3Operators + TestPartialReduction); explicit rule-4 happy-path covered by the canonical examples in `TestDoc07Validation::test_example_2_supported` (uses `{0,3}` gap) | DEC-006 |
| REQ:06.rule-5 | Rule 5: Polarity support — `UNSUPPORTED_POLARITY` | `src/validation/validator.py::_rule_5_polarity` (polarity_support is True under v0.1 → polarity always passes; rule is in place for capability-toggle future) | `tests/unit/test_validator.py::TestRule5Polarity::test_polarity_supported_in_mvp` | DEC-006, DEC-011 |
| REQ:06.rule-6 | Rule 6: Inverse support — `UNSUPPORTED_INVERSE` | `src/validation/validator.py::_rule_6_inverse` (rejects InverseExpr under v0.1) | `tests/unit/test_validator.py::TestRule6Inverse::test_inverse_unsupported`; `tests/unit/test_validator.py::TestDoc07Validation::test_example_5_unsupported_inverse` | DEC-006 |
| REQ:06.rule-7 | Rule 7: Expansion support — `UNSUPPORTED_EXPANSION` | `src/validation/validator.py::_rule_7_expansion` | `tests/unit/test_validator.py::TestRule7Expansion::test_expansion_unsupported`; `tests/unit/test_validator.py::TestDoc07Validation::test_example_6_partial_expansion`; `tests/unit/test_validator.py::TestPartialReduction::test_expansion_stripped` | DEC-006 |
| REQ:06.rule-8 | Rule 8: Compound node support — `UNSUPPORTED_COMPOUND_NODE` | `src/validation/validator.py::_rule_8_compound_nodes` (compound_node_support=False under v0.1) | `tests/unit/test_validator.py::TestRule8CompoundNodes::test_compound_unsupported` | DEC-006 |
| REQ:06.rule-9 | Rule 9: Match mode support — `UNSUPPORTED_MATCH_MODE` | `src/validation/validator.py::_rule_9_match_mode` | `tests/unit/test_validator.py` (covered indirectly via canonical examples; explicit unsupported-mode case is unreachable today because parser only emits modes that are in the v0.1 registry — rule wired as defense-in-depth) | DEC-006, DEC-010 |
| REQ:06.rule-10 | Rule 10: Scope validation — `UNSUPPORTED_SCOPE_FIELD`, `UNKNOWN_CORPUS`, `UNKNOWN_LANGUAGE`. **Slice L extends rule 10** with `WINDOW_EXCEEDS_MAX` (error; lands in the "unreducible errors" set so status=unsupported, NOT partial — L-CLOSE-001) and `GAP_NARROWED_BY_WINDOW` (informational warning; treated alongside RULE13_PRIOR_GROUNDED so status stays supported). | `src/validation/validator.py::_rule_10_scope`; `src/validation/validator.py::validate` (unreducible_error_codes branch) | `tests/unit/test_validator.py::TestRule10Scope::test_unknown_corpus`, `::test_unknown_language`, `::test_valid_scope`; `tests/unit/test_validator.py::TestRule10WindowConstraints::test_window_within_limit`, `::test_window_exceeds_max` (asserts status="unsupported"), `::test_gap_narrowed_by_window_warning` (asserts status="supported"); `tests/unit/test_validator.py::TestDoc07Validation::test_example_7_unsupported_root_hebrew` (unknown-language path) | DEC-006, DEC-094 |
| REQ:06.rule-11 | Rule 11: Sequence length — `SEQUENCE_TOO_LONG` | `src/validation/validator.py::_rule_11_sequence_length` (max_sequence_length=10 under v0.1) | `tests/unit/test_validator.py::TestRule11SequenceLength::test_within_limit`, `::test_exceeds_limit` | DEC-006 |
| REQ:06.rule-12 | Rule 12: Structural validation — `EMPTY_SEQUENCE`, `MALFORMED_AST`, `INVALID_COMPOUND` | `src/validation/validator.py::_rule_12_structural` | `tests/unit/test_validator.py::TestDoc07Validation` (well-formed examples pass rule 12); parser-level structural enforcement covers empty-input via `tests/unit/test_parser.py::TestParseFull::test_empty_input_raises` so rule 12 receives well-formed plans by construction | DEC-006 |
| _— Canonical 08 (MVP Corpus Scope) — added 2026-05-23 audit —_ | | | | |
| REQ:08.concept-registry | Concept registry — initial set (~20 concepts, ~30 lemma mappings), seeded at `origin='curated'`, `verification_state='unverified'`, `confidence=NULL` per registry-epistemics invariants | `data/schemas/02_concept_registry.sql` (concepts + concept_lemmas tables); `data/seeds/registry/concepts.csv`, `concept_lemmas.csv` (curated seed data); `scripts/db/seed_registry.py` (transactional seed CLI); `src/ontology/registry.py::ConceptRegistry` (reader: `get_by_lemma`, `get_lemmas_for_concept`, `empty()`); `src/app/routes/concepts.py` (`GET /api/v1/concepts` exposes the registry per DEC-076) | `tests/unit/test_ontology_registry.py` (27 tests across reader behavior); `tests/integration/test_concept_registry_seed.py` (10 tests including Slice C exit gate); `tests/integration/test_concept_registry_reader.py` (14 tests); `tests/unit/test_app_routes_concepts.py` (HTTP surface) | DEC-024, DEC-049, DEC-076 |
| REQ:08.concept-table | `concepts` table: `id`, `name UNIQUE`, `description`, `origin`, `verification_state`. Polarity is **not** a column — it's a claim (see polarity_claims). | `data/schemas/02_concept_registry.sql` (concepts table with CHECK constraints on `origin` and `verification_state`); `src/ontology/registry.py::concepts_table` (Core mirror) + `Concept` Pydantic | `tests/integration/test_apply_schemas.py` (CHECK constraint coverage on `origin` and `verification_state` for concepts); `tests/integration/test_concept_registry_seed.py::test_all_concepts_have_unverified_state` | DEC-024, DEC-049 |
| REQ:08.concept-lemma-table | `concept_lemmas` table: lemma↔concept mapping with `confidence DEFAULT NULL`, provenance, `UNIQUE (lemma, language, concept_id)` | `data/schemas/02_concept_registry.sql` (concept_lemmas table); `src/ontology/registry.py::concept_lemmas_table` (Core mirror) + `ConceptLemma` Pydantic; `src/ontology/registry.py::ConceptRegistry.get_lemmas_for_concept` (the read-side) | `tests/integration/test_apply_schemas.py::test_concept_lemmas_unique_constraint` and value-domain CHECKs; `tests/integration/test_concept_registry_reader.py::test_get_lemmas_for_concept_*` (5+ tests) | DEC-024, DEC-049 |
| _— Canonical 09 (Backend Service Boundaries) — added 2026-05-23 audit —_ | | | | |
| REQ:09.dsl-parser | Hand-written recursive-descent parser: `parse(dsl: str) -> QueryPlan`, raises `ParseError` with line/column on invalid syntax | `src/engine/parser.py` (tokenizer `tokenize()`; `_Parser` recursive-descent class with parse_step / parse_sequence / parse_alternative / parse_optional / parse_directives / _parse_typed_value / _parse_polarity / _parse_gap_constraint / _parse_inverse_function helpers; module-level `parse()` entry point; `ParseError` exception with position) | `tests/unit/test_parser.py` (68 tests across TestTokenize, TestParseNodeRef, TestParseSequence, TestParseAlternative, TestParseOptional, TestParseNegation, TestParseDirectives, TestParseInverse, TestParseFull, TestDoc07Examples) | DEC-008, DEC-009 |
| REQ:09.capability-validator | Validator component (canonical-09 §4 home): `validate(plan, registry) -> ValidationResult` running sequential rule checks against the capability registry | `src/validation/validator.py::validate` (entry point that walks `_RULES` list + appends rule 13 grounding warning); `src/validation/registry.py::CapabilityRegistry.mvp()` (the registry the validator checks against); `src/app/routes/validate.py` (HTTP surface). Same logical component as REQ:06.* rules. | `tests/unit/test_validator.py` (60+ tests across all 12 + rule-13 paths + partial-reduction + canonical-07 examples); `tests/unit/test_rule_13_registry_grounding.py`; `tests/unit/test_app_routes_validate.py` | DEC-006, DEC-049, DEC-079 |
| REQ:09.scoring-ranking | Scoring & ranking component: `score(candidates, prefs) -> list[ScoredMatch]`. **Not yet implemented** — MVP boundary. | _Not yet implemented._ `src/scoring/__init__.py` is an empty package marker. The contract-slot exists via `src/engine/models.py::RankingPrefs`, `RankingFactor`, and `ExplainedResult.score: float \| None = None` (per DEC-061). The `summary` clarifies that scoring ranks within a result set; calibration vs alternatives is the contextualization layer (REQ:09.contextualization, already shipped). | _No tests — the slot exists but no behavior is wired._ Lands when the scoring slice begins. | DEC-019, DEC-054, DEC-061 — deferred to post-MVP scoring slice |

_Run `/coverage` to refresh this table from `<!-- REQ:... -->` markers in `docs/canonical/`. (Note: incremental `/review` adds rows for slice-touched REQs; the full-audit `/coverage` run is what populates the canonical-doc-wide table you see above. The 2026-05-23 audit is the first full run.)_

## Gaps

### Specced but not coded
- **REQ:09.scoring-ranking**: canonical-09 §7 boundary clarified at Slice D close (DEC-054); no implementation. `src/scoring/` is empty. Lands when scoring slice begins.
- **REQ:03.scoring-philosophy**: same root as REQ:09.scoring-ranking — same deferral.
- **REQ:04.node-root**: v0.2 per canonical-04 versioning; parser surface exists, validator + executor reject under v0.1. Lands with v0.2 capability uplift.
- **REQ:04.node-domain**: v0.2 per canonical-04 versioning; parser surface exists, validator + executor reject under v0.1. Lands with v0.2 capability uplift.
- _Closed 2026-05-09 (Slice G): REQ:09.request-lifecycle gap — the 11-step lifecycle now has a coverage row above; the as-shipped HTTP flow realizes it end-to-end._

_Slice-D-tracked drift (REQ:04.matching-rules pre-Slice-D underscored helpers) closed at Slice D close. Slice-F-tracked drift (REQ:09.result-explainer pre-Slice-F `list[ScoredMatch]` signature + LLM-conceptual claim) closed by canonical-09 amendment + DEC-061._

### Partial — parser surface present, validator/executor rejects under v0.1
These REQs have AST + parser support but the v0.1 capability registry refuses them; they ride to "covered" status when the capability registry is bumped to v0.2 (root/domain/inverse/expansion/compound). No additional work required at the parser layer.
- REQ:05.inverse-expr (validator rule 6)
- REQ:05.expansion-directive (validator rule 7)
- REQ:02.expansion-syntax (same root as REQ:05.expansion-directive at the surface layer)
- REQ:04.node-root (validator rule 2 + executor refusal)
- REQ:04.node-domain (validator rule 2 + executor refusal)
- REQ:04.compound-nodes (validator rule 8)

### Coded but not tested
_None identified by 2026-05-23 audit._ Spot checks across the 60 newly-added rows found every REQ with code has at least one test reference. Most of the surplus is the opposite — tests exist that weren't being credited because the row didn't exist.

### Flagged for user disposition
The 2026-05-23 audit flagged the following items for follow-up that are NOT immediate-fix coverage gaps:

1. **REQ:01.job-to-be-done as umbrella row**: this row composes the entire NL→DSL→validate→execute→explain pipeline. Listed because it has a canonical marker; the "real" coverage lives across REQ:09.* rows. Recommend keeping the umbrella row as-is — it points back at the constituent REQs.
2. **REQ:06.rule-4 (gap constraints), rule-5 (polarity), rule-9 (match mode), rule-12 (structural)**: these rules exist in `_RULES` but the v0.1 capability registry sets the corresponding flags to permissive defaults (gap unbounded, polarity supported, modes all listed, well-formed by parser construction), so the rules are wired-but-rarely-fire defense-in-depth. The test coverage above is light because the v0.1 inputs don't exercise the negative path. Recommend adding a `# defense-in-depth` annotation in each rule body and a deliberate negative-path test next time the capability registry tightens. Low priority.
3. **REQ:03.retrieval-strategy**: only stage 1 (symbolic) is implemented. Lexical + semantic + rerank stages are post-MVP. Row reflects this honestly; lands when an agent slice or scoring slice adds new stages.
4. **`src/scoring/` empty package**: `src/scoring/__init__.py` exists as a marker. Confirm at scoring-slice kickoff whether to keep the empty marker or remove until the slice lands. Cosmetic.

## Audit Process Notes (2026-05-23)

This audit was driven by the hygiene-audit-2026-05-23.md finding that the Summary block said "Requirements identified: 0" because incremental `/review` populated rows only for slice-touched REQs. The audit:

1. Enumerated all 79 unique `<!-- REQ:XX.* -->` markers in `docs/canonical/*.md`.
2. Cross-referenced against the 19 pre-existing rows.
3. For the 60 unreferenced REQs, mapped each to `src/` + `tests/` by reading canonical-doc context (section text immediately following the marker) and matching against the live file tree.
4. Preserved the 19 pre-existing rows verbatim — they were richer than a programmatic re-scan would produce.
5. Updated the Summary block + Gaps section with real counts and recategorized closed items.

The matrix above is the first artifact in the project's history that maps every canonical REQ marker to code + tests. Subsequent slices should hand-update touched rows during `/review` (as they have been); the full re-audit should run once per major milestone to catch drift.
