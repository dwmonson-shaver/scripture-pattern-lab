# Independent Reviews Log

Index of independent code and design reviews. Each entry links to the review
artifact in `docs/reviews/`.

Independent review is an explicit Quality Gate at slice-close (CLAUDE.md): a
slice is not declared closed until a Codex pass has run on the cumulative
slice diff, the artifact is committed to `docs/reviews/`, this log has a new
row, and any P0/P1/P2 findings have been addressed.

## Reviews

| Date | Review | Scope | Verdict | Findings | Closure |
|------|--------|-------|---------|----------|---------|
| 2026-05-02 | [Codex code (Slice A)](../reviews/review-codex-code-slice-a-2026-05-02.md) | Slice A — corpus ingestion 3-John end-to-end | needs-attention | 0 P0, 0 P1, 3 P2 | All closed pre-Slice-B as Bucket 1: `9f39f25` (tokenize wildcard `*`), `9aa900a` (parser polarity-on-parens), `6d6af2a` (validator composite recursion). DECs DEC-031, DEC-032, DEC-033. |
| 2026-05-02 | [Codex adversarial design (Slice A)](../reviews/review-codex-adversarial-design-slice-a-2026-05-02.md) | Slice A — corpus design + canonical-09 boundary, adversarial pass | needs-attention | 4 (2 high, 2 medium) | Design #4 (canonical-09 ingestion section) closed by `4da88f6` (REQ:09.ingestion added). Design #1+#2 (registry epistemics + validator extension) → **Bucket 2** — Trigger: at Slice C start (epistemic-discipline backlog items 3–5). Rationale: closing them requires canonical-08 REQ markers + a `/design` for registry epistemics + validator code changes — three coordinated artifacts that belong to Slice C's scope (concept registry seeding); doing them in Slice B would be a horizontal cross-cut. Slice B doesn't ship conceptual/polarity behavior, so Codex's no-ship verdict doesn't fire on Slice B. **[Triage 2026-05-03 — pre-Slice-C, by assistant]** Disposition: **scope in**. Slice C's `/research` and `/design` will fold in (a) `REQ:08.registry-epistemics` in `docs/canonical/08_mvp-corpus-scope.md` (backlog item 3); (b) `/design` discussion for registry epistemics — prior-grounded vs evidence-grounded match types, provenance/evidence schema, default-confidence semantics (backlog item 4); (c) capability-validator extension that flags polarity-marked concept queries against unverified registry entries as prior-grounded rather than supported (backlog item 5). **[Update 2026-05-03 — design phase, by assistant]** Slice C `/design` for registry epistemics landed at `thoughts/design-registry-epistemics-2026-05-03.md` (status: approved). 8 decisions + 6 open questions resolved. Path B selected: Slice C **expands** to a second design track for **result contextualization** (anti-confirmation-bias species 2 — observed pattern count vs alternative orderings vs null-distribution baseline), addressing a gap in canonical specs the user surfaced during design review (canonical specs commit to finding/ranking matches but not to contextualizing them against baselines). Bucket 2 closure now requires: both design artifacts + `REQ:08.registry-epistemics` + new contextualization REQ marker (id TBD) + validator/scoring code realizing both. Closing SHA pending Slice C close. **[Update 2026-05-08 — slice re-scope, by orchestrator]** Track 2 (result contextualization) design landed at `thoughts/design-result-contextualization-2026-05-03.md` (status: design-stable-implementation-deferred). OQs #1–#4 resolved in-walkthrough (middle-path default; retrieval-side code home; defer null-distribution; cap=24). OQs #5–#6 deferred-pending-interface. **Slice C re-scoped**: ships Track 1 (registry epistemics) implementation + `src/engine/executor.py` (was Bucket 3 — see disposition flip below) + thin CLI `scripts/query.py` so the user can run real queries against the seeded registry. Track 2 implementation moves to Slice D with sharpened trigger: *after CLI ships and the user has interacted with real result counts* — calibration-shape decisions (OQs #5, #6) require seeing actual `MatchCandidate` output, which doesn't exist until the executor + CLI land. Rationale: the user surfaced mid-walkthrough that they cannot meaningfully evaluate calibration shapes for results that don't yet exist; deferring Track 2 implementation is honest about the feedback-loop gap. Bucket 2 closure now requires: ✓ Track 1 design (approved 2026-05-03), ✓ Track 2 design (design-stable 2026-05-08), Track 1 code in Slice C, Track 2 code in Slice D. Closing SHA pending Slice D close (not Slice C close). **[Update 2026-05-09 — Slice C close, by orchestrator]** Track 1 code is **complete and slice-closed**: registry epistemics structurally realizes DEC-024 across canonical-08, the four-table schema with CHECK constraints, the SQLAlchemy + Pydantic mirrors, the read-only `ConceptRegistry` reader, the validator's rule-13 grounding axis, and the seed script with its `unverified`-by-default invariant. Track 1 closing SHA chain: `9251a11`…`99d4214` (Phases 1–6 + interim P1/P2 closure). Track 2 (result contextualization) implementation remains scoped to **Slice D** with sharpened trigger ("after CLI ships and the user has interacted with real result counts"). Per DEC-051, Track 2 design is design-stable and implementation-deferred. Bucket 2 stays open until Slice D ships Track 2 code; this row's final closing SHA lands when Slice D closes. **[Triage 2026-05-09 — Slice D start, by orchestrator]** Disposition: **scope in**. Trigger fired (Slice C closed, CLI ships, real result counts now observable via `scripts/query.py "faith > hope > love"`). Slice D's `/research` and `/structure` use `thoughts/design-result-contextualization-2026-05-03.md` (status: design-stable) as the load-bearing input. Implementation phases: D1 `REQ:09.contextualization` marker + canonical-09 amendment; D2 Pydantic models (`NodeBaseline`, `AlternativeOrderingCount`, `NullDistribution`, `Contextualization`, real `ExplainedResultSet`); D3 baseline computation in `src/retrieval/contextualization.py`; D4 alternative-ordering computation (cap `min(N!, 24)` + single-swap fallback for N≥5); D5 `contextualize()` orchestrator + `src/retrieval/retrieve.py` wrapper (engine-layer default-off, CLI/API default-on per OQ #1 middle-path resolution); D6 CLI extension + slice-exit gate `test_full_corpus_contextualization_faith_hope_love`. Null-distribution = schema slot only, always-`None` in MVP (per OQ #3 resolution). OQs #5 (canonical-01 amendment) and #6 (Pydantic dump shape) remain deferred-pending-interface; user reacts to real output at slice close. Closing SHA pending Slice D close. Design #3 (book-id normalization: BB digits in `tokens.book` vs `book:rom` abbrev form in DSL examples) → **Bucket 3** — Trigger: before any pattern-engine work that consumes the DSL `book:` constraint. Rationale: Slices A–C don't execute DSL queries against `tokens.book`, so the mismatch isn't exercised yet; adding a normalization layer now would be premature without the actual call site to design against. **[Triage 2026-05-03 — pre-Slice-C, by assistant]** Disposition: **re-defer**. Trigger reaffirmed and sharpened: *before pattern-engine work in `src/engine/executor.py` that consumes the DSL `book:` constraint against `tokens.book`*. Rationale (specific to Slice C): Slice C is concept-registry seeding — it writes to `concepts` / `concept_lemmas` / `concept_inverse` tables and does not execute DSL queries against `tokens.book`, so the BB-digit ↔ abbreviation mismatch is not exercised yet. Adding a normalization layer now would be premature without the actual call site to design against. Original trigger remains specific and accurate; this is not a stale "eventually" re-deferral. **[Update 2026-05-08 — Slice C re-scope, by orchestrator]** Disposition flipped: **scope in** to Slice C. Rationale: Slice C re-scope (see Bucket 2 update above) pulls `src/engine/executor.py` into this slice as the prerequisite for the user-runnable CLI. The original trigger now fires. Bucket 3 closure requires the executor design to address book-id normalization between DSL `book:rom` form and `tokens.book` BB-digit form, plus the resolution code itself. **[Closure 2026-05-09]** **Bucket 3 CLOSED** — closing SHAs `0eef8f5` (introduces `src/ontology/book_codes.py` with the abbrev↔BB↔display map + `book_abbrev_to_bb` / `bb_to_display` helpers, both raising on unknown; closes the silent-miss gap with `test_canonical_07_examples_all_resolve` as the discipline guard) and `ca4b4e8` (executor consumes `book_abbrev_to_bb` at WHERE-clause build time + integration test `test_book_filter_normalizes_abbreviation` exercises the full path). DEC-053 records the placement decision (`src/ontology/` not `src/engine/`, so the future explainer can reuse the same map). |
| 2026-05-03 | [Codex code (Slice B close)](../reviews/review-codex-code-slice-b-close-2026-05-03.md) | Slice B — full ingestion CLI + observability + smoke test | minor-fixes-recommended | 0 P0, 0 P1, 0 P2, 2 P3, 1 info | Both P3s closed by `069a923` (fixture-edge ordering on `test_full_corpus_smoke`; `_FakeEngine.in_transaction` post-commit assertion in `test_callback_emits_done_with_final_count`). Info note acknowledged (subprocess env inheritance pattern is sound). |
| 2026-05-08 | [Codex code (Slice C Track 1 interim)](../reviews/review-codex-code-slice-c-track-1-2026-05-08.md) | Slice C Track 1 — registry epistemics, 6 phases (≈2,439 lines across 15 files); base SHA `fed3b98` | FAIL → clean after fix | 0 P0, 1 P1, 1 P2, 0 P3, 0 info | Both findings closed inline before executor work begins. **P1 — `seed_registry.py` non-empty gate could mutate a foreign registry whose row count happens to match the CSV**: closed by tightening the gate to a strict `existing > 0 → refuse` predicate matching `ingest_corpus.py`. The `test_seed_is_idempotent` test (which depended on the relaxed predicate) was renamed `test_seed_is_reproducible_via_truncate` and rewritten to drive idempotency through `--truncate + reseed`. **P2 — SQL schema did not enforce declared value domains for `origin`, `verification_state`, `polarity`, `evidence_count`, or `confidence`**: closed by adding DB-level CHECK constraints to `data/schemas/02_concept_registry.sql` (`origin IN (...)`, `verification_state IN (...)`, `polarity IN ('+','-','±')`, `evidence_count >= 0`, `confidence IS NULL OR confidence BETWEEN 0 AND 1`). Mirrored the same checks in `src/ontology/registry.py` Table definitions; added 5 new IntegrityError-raising tests in `tests/integration/test_apply_schemas.py`. Closing SHA `99d4214`. |
| 2026-05-09 | [Codex code (Slice C close)](../reviews/review-codex-code-slice-c-close-2026-05-09.md) | Slice C close — full slice diff fed3b98..15a6bb1 (executor + CLI emphasis): 26 files changed, ≈4,489 lines; base SHA `fed3b98` | FAIL → clean after fix | 1 P0, 1 P1, 3 P2, 1 P3, 0 info | All six findings closed inline; closing SHA `1a2cf93`. **P0 — negated NodeRefs silently matched as positive** (executor's `_validate_plan_shape` accepted `NodeRef.negated=True` and the resolution path ignored the modifier): closed by raising `UnsupportedPlanShape` on negated steps + 2 new unit tests. **P1 — unbounded per-candidate SELECT fan-out** (one SELECT per surviving step-1 chain per later step → thousands of serial queries on common first lemmas): closed by replacing the per-candidate loop with `_extend_chains_one_step` — one SELECT per step using `tuple_(book, chapter, verse).in_(...)` against the union of surviving verses, with the join + gap evaluated in Python. Iterative-per-step algorithm preserved per canonical-09 §5; only the per-step batching changed. **P2 — incomplete shape gate** (no rejection of `morph_filters` non-empty; no `len(operators) == len(steps) - 1` check): closed with three new raise paths + tests. **P2 — scope filters only on step 0** (later-step queries didn't filter by `corpus_id` or `language` — invisible today with one corpus loaded but a leak waiting for OT/LXX): closed by carrying scope WHERE clauses into every step query + 2 new compile-statement assertions. **P2 — DEC-025 boundary breach** (`src/engine/executor.py` imported `tokens_table` from `src/ingestion/db.py`): closed by introducing `src/engine/_schema.py` private mirror (DEC-052); grep verifies zero `src.ingestion` imports under `src/engine/`. **P3 — CLI exit-3 unreachable for unmapped concepts** (registry-returns-`[]` silently produced zero matches instead of the documented exit code): closed via new `ConceptNotMapped` exception raised by `_resolve_step_lemmas`, caught by `scripts/query.py::main` for exit 3 + naming the offending concept. Slice exit gate `test_full_corpus_faith_hope_love_in_1cor_13_13` still green; manual smoke `scripts/query.py "faith > hope > love"` still returns the two `1Cor 13:13` candidates with `Status: supported   Match type: conceptual`. |
| 2026-05-09 | [Codex code (Slice D D1+D2 checkpoint)](../reviews/review-codex-code-slice-d-d1d2-checkpoint-2026-05-09.md) | Slice D D1+D2 — REQ:09.contextualization marker + canonical-09 amendment + Pydantic models; 2 commits `0f8b553`..`cbd27b5`; base SHA `5276462` | needs-attention → clean after fix | 0 P0, 0 P1, 2 P2, 1 P3, 0 info | All three findings closed inline; closing SHA `e3d557b`. **P2 — request lifecycle omitted contextualization step** (canonical-09 §request-lifecycle still went straight from "Retrieval pipeline returns MatchCandidates" to "Scoring ranks candidates"): closed by inserting steps 7–10 that name `RetrievalResult`, the contextualization step, and the `ExplainedResultSet` envelope. **P2 — Pydantic count/index invariants documented but unenforced** (`int`/`float` allowed negatives): closed by switching to `NonNegativeInt`/`NonNegativeFloat` for `node_index`, `count`, `sample_size`, `std`, `observed_count`, and `permutation` items, plus 7 negative-value rejection tests. **P3 — directory map omitted `src/retrieval/contextualization.py`**: closed by adding the line under `retrieval/` in canonical-09's directory map. |
| 2026-05-09 | [Codex code (Slice D D3+D4 checkpoint)](../reviews/review-codex-code-slice-d-d3d4-checkpoint-2026-05-09.md) | Slice D D3+D4 — `compute_node_baselines` + `compute_alternative_orderings` + executor helper promotion; 3 commits `e3d557b`..`d48aaca`; base SHA `cbd27b5` | clean (1 P3 noted) | 0 P0, 0 P1, 0 P2, 1 P3, 0 info | Closed inline; closing SHA `d52b491`. **P3 — `_fallback_permutations(n)` could exceed canonical-09 §8's stated `min(N!, 24)` ceiling for direct-call plans whose N ≥ 23**: closed by truncating the fallback list at `_MAX_PERMUTATIONS = 24` plus a boundary test at N=30. In normal MVP traffic the validator caps sequences at 10 steps so the fallback gave 11 perms — well below the cap; the fix protects against direct-call `QueryPlan` instances bypassing the validator. |
| 2026-05-09 | [Codex code (Slice D close)](../reviews/review-codex-code-slice-d-close-2026-05-09.md) | Slice D close — full slice diff `5276462..44fc697` (8 commits): REQ:09.contextualization end-to-end. ~13 files changed across canonical-09, src/engine/models.py, src/engine/executor.py, src/retrieval/contextualization.py, src/retrieval/retrieve.py, scripts/query.py, plus 5 test files | minor-fixes-recommended → clean after fix | 0 P0, 0 P1, 0 P2, 3 P3, 0 info | Two of three closed inline at `37ed192`; one bucketed. **P3 (D-CLOSE-001) — canonical `retrieve()` and `contextualize()` signatures omitted scope/engine/registry** that the shipped implementation requires: closed by expanding both signatures in canonical-09 §6 and §8 to include the actual DI parameters with their MVP defaults. **P3 (D-CLOSE-002) — invariant (b) said "N pairwise swaps" while code does (N−1) adjacent swaps truncated at 24**: closed by amending the invariant text. **P3 (D-CLOSE-003) — CLI's `_print_contextualization` is unbounded for very long resolved-lemma lists and sequence labels** (no truncation/wrapping policy): **deferred → Bucket 4 — CLI rendering polish.** Trigger: the next slice that touches CLI output discipline — most likely the explainer slice (which adds prose rendering on top of the contextualization envelope), or any slice that introduces a concept whose resolved-lemma list exceeds ~5 items. Rationale: the current seed concepts produce readable output (longest resolved-lemma list is `[πίστις, πιστεύω]` for `faith`); a truncation/wrapping policy without a real overrun case would be premature. The fix is well-scoped (cap or wrap resolved-lemma display, e.g. "first 5 lemmas + N more"; apply max-width policy to `sequence_label`) but needs a concrete-output target to design against. Slice exit gate `test_cli_renders_contextualization_for_flagship_sequence` is green; manual smoke confirms the slice's stop condition (`scripts/query.py "faith > hope > love"` prints 2 candidate matches at 1Cor 13:13 plus the Contextualization envelope showing faith/hope/love baselines and 6 alternative-ordering counts). Slice D test counts: 285 unit (was 237 at Slice C close) + 68 integration (was 59) = +48 unit + 9 integration tests. **Bucket 2 (Track 2 result contextualization) ✅ CLOSED** — closing SHA chain `0f8b553` … `44fc697` (D1–D6) plus `37ed192` (slice-close P3 closures). **Bucket 4 OPEN** — described in this same closure column (above). |
| 2026-05-09 | [Claude code, fallback (Slice E close)](../reviews/review-claude-code-slice-e-close-2026-05-09.md) | Slice E close — full slice diff `4e57cf0..4e43609` (5 commits): doc-only slice. New: `docs/agent/dsl-cookbook.md` (501 lines), `docs/agent/prompt-template.md` (84 lines), `.claude/commands/orchestrate-slice.md` (253 lines). No source-code changes. | needs-attention → clean after fix | 0 P0, 3 P1, 3 P2, 3 P3, 3 info | **NOTE on reviewer:** Codex was the intended reviewer but blocked by a `/Users/dwmonson/.codex` session-directory permission issue (`sudo chown -R $(whoami) /Users/dwmonson/.codex` required from the user). A fallback independent review by a `general-purpose` Claude agent ran the same severity language and the same checklist; review artifact saved alongside the Codex naming convention but with `flavor: claude-fallback`. **Bucket 5 — re-run Codex once `.codex` permissions are fixed** so the slice carries an authoritative second pass; trigger: next time the user runs `/codex:setup` or any `/codex:rescue` succeeds against this repo. **Findings — all closed inline at `9e08096`.** Three P1 stderr-recognition / capability-classification fixes in the cookbook's quick-reference table and failure-mode catalog: **E-CLOSE-001** (`UnsupportedPlanShape` block was multi-line; code prints single line `executor rejected plan: <msg> (path=<jsonpath>)`); **E-CLOSE-002** (`Status: unsupported` recognition prefix was wrong — code prints `validator rejected plan: status=unsupported`); **E-CLOSE-003** (polarity was documented as "validator rejects" but MVP `polarity_support=True` so polarity actually passes both validator and executor — cookbook now warns agents that polarity is a no-op for matching in MVP and removes `UNSUPPORTED_POLARITY` from the validator-error table). Three P2 stderr-recognition fixes: **E-CLOSE-004** (`concept not mapped` quote replaced with the actual full message); **E-CLOSE-005** (`RegistryRequired` failure mode renamed to `registry not seeded` to match what the CLI actually prints); **E-CLOSE-006** (zero-match output corrected — `Found 0 matches.` with period, no `(showing first 0):` suffix; added note that `Match type:` line is omitted for 0-match results). Three P3 polish: **E-CLOSE-007** (quick-reference polarity row consistency, fixed by E-CLOSE-003); **E-CLOSE-008** (Worked Example 1 now explains why two candidates can share the same verse+positions — multiple chain paths through the executor's in-memory join); **E-CLOSE-009** (operator rows differentiated: `>>` passes validator and executor rejects; `~` validator rejects). Three info notes acknowledged in the review artifact (E-CLOSE-010 covered by E-CLOSE-006 fix; E-CLOSE-011 and E-CLOSE-012 are confirming notes — no fix needed). **Slice E exit gate**: a fresh `general-purpose` agent given only the cookbook + prompt template authored syntactically valid DSL for the 3 representative research questions and recovered correctly from the unset-`DATABASE_URL` failure mode, but live-corpus end-to-end verification (real verse references, real counts) was not possible in this session because `DATABASE_URL` was not set. **Bucket 6 — Slice E live-corpus verification** with the same 3 research questions as the next time the user starts the corpus DB. Trigger: any session in which `DATABASE_URL` is set against a fresh agent reading only the cookbook. Rationale: validates the half of the exit-gate rubric requiring ground-truth corpus answers; this slice's fixes pre-emptively addressed the offline-detectable cookbook gaps. **Bucket 4 (CLI rendering polish) re-deferred at Slice E start** — Slice E documents CLI output but does not modify rendering code; sharpened trigger: "next slice that *modifies* `_print_contextualization` or any CLI rendering code." Closing SHA chain: `63fe651` (E1) → `cff4b11` (E2) → `52b60b1` (E3) → `3fdf44c` (E4) → `4e43609` (E5 fix) → `9e08096` (slice-close findings closed). DEC-060 records the agent-facing-docs directory boundary. |
| 2026-05-09 | [Claude code, fallback (Slice F F4+F5 checkpoint)](../reviews/review-claude-code-slice-f-f4f5-checkpoint-2026-05-09.md) | Slice F mid-slice — F4+F5 cumulative `git diff fd46c5d..5e8bf1b` (2 commits): `src/engine/models.py` (ExplainedResult/ExplainedResultSet) + `src/nlp/explainer.py` + 39 unit tests | minor-fixes-recommended → clean after fix | 0 P0, 0 P1, 1 P2, 4 P3, 3 info | Codex blocked by same `.codex` permission issue (Bucket 5 still open at this checkpoint). Claude fallback ran the same checklist. **All closed inline at `18d6948` before F6 began.** P2 — F-F4F5-001 (4 missing tests for InverseExpr label, nl_source round-trip, text_display populated/empty): closed by adding TestExplainEdgePaths with 4 tests. P3 — F-F4F5-002 (n=1 redundant singularity line) + F-F4F5-003 (multi-verse-under-cap repeats count): both closed by guarding line-2 emission on `n > 1` AND `len(refs) > _VERSE_LIST_CAP`; TestPoseQualityFixes added with 3 confirming tests. P3 — F-F4F5-004 (variant match_type unexercised): closed by TestVariantMatchType. Info — F-F4F5-005 (caller-contract divergence between ctx.observed_count and len(candidates)): closed by adding docstring note to explain() warning callers. Info — F-F4F5-006 (test bound looser than implementation): tightened from `<= 6` to `<= 5` to match actual cap. Info — F-F4F5-007 (empty resolved_lemmas pathological state): unreachable on a successfully executed plan; acknowledged, no code change. |
| 2026-05-09 | [Claude code, fallback (Slice F close)](../reviews/review-codex-code-slice-f-close-2026-05-09.md) | Slice F close — full slice diff `fd46c5d..de364a8` (5 commits): `src/engine/models.py` (ExplainedResult/Set) + `src/nlp/explainer.py` + `scripts/query.py` (CLI integration + --no-prose) + canonical-09 §9 amendment + 50+ tests across unit + integration. The deterministic explainer module + DEC-061 LLM-deferral. | pass-with-conditions → clean after fix | 0 P0, 0 P1, 0 P2, 2 P3, 1 info | Codex still blocked by `.codex` permissions; Claude fallback ran the same six-category checklist. **Bucket 5 (re-run Codex on Slice E + F) remains open** — same trigger as before. **All P3s closed inline at `<governance-commit-SHA-pending>`.** F-CLOSE-001 (canonical-09 §9 summary field comment said "≤ 5 lines per invariant (e)" but invariant (e) is about lemma/label cap policy, not line count): closed by rewording to "≤ 5 lines; see MVP implementation note below." F-CLOSE-002 (DEC-061 named "Bucket 7" but no bucket entry existed in this log): closed by this row's Bucket 7 declaration. F-CLOSE-003 info (exit-gate test doesn't assert prose indentation): acknowledged, no fix — risk is low and a contrived assertion would be brittle. **Bucket 4 (CLI rendering polish) ✅ CLOSED** — closing SHA chain `5e8bf1b` (introduces `_truncate_lemmas` cap=5 with "+N more" suffix and `_truncate_sequence_label` cap=64 chars with ellipsis in `src/nlp/explainer.py`) + `707669b` (CLI invokes explain() with the prose-layer caps applied; structured `_print_contextualization` deliberately remains unbounded as the data-fidelity view) + `de364a8` (canonical-09 §9 invariant (e) codifies the cap-in-prose / unbounded-in-structured split). **Bucket 7 NEW — LLM-backed conceptual-match prose**. Trigger: Slice H ships an LLM dependency for translation OR the deterministic explainer prose is judged inadequate against a real research question (whichever fires first). Rationale (DEC-061): adding an LLM client solely for prose generation is overkill before the user has seen a deterministic baseline. Slice H is the natural home for the project's first LLM dep because translation is impossible without it; explanation is not. Bucket closes when (a) Slice H lands an Anthropic/OpenAI/etc. client AND the explainer optionally consumes it for conceptual matches, OR (b) the user explicitly evaluates the deterministic prose against a research question and the verdict is "inadequate," at which point a focused slice resolves it. **Slice F exit gate**: `test_cli_renders_explanation_for_flagship_sequence` in `tests/integration/test_query_cli.py` — a real `scripts/query.py "faith > hope > love"` run produces an `Explanation:` block AFTER the contextualization block, with grounded counts (483/84/259), verse refs (1Cor 13:13), and an alt-ordering observation. Test gated `pytest.mark.integration` so it skips when DATABASE_URL isn't set. Slice F unit-test counts: 340 total (was 293 at Slice D close, +47 = 10 model + 29 + 8 explainer phase fixes); integration tests +2 (was 9, now 11). Closing SHA chain: `62c5fd3` (F4) → `5e8bf1b` (F5) → `18d6948` (F5b checkpoint closures) → `707669b` (F6) → `de364a8` (F7) → `<governance-SHA>` (F-close). DEC-061 records both divergences (RetrievalResult signature + LLM deferral). |
| 2026-05-09 | [Codex code (Slice G G1+G2+G3+G4 checkpoint)](../reviews/review-codex-code-slice-g-g1g2g3g4-checkpoint-2026-05-09.md) | Slice G mid-slice — G1+G2+G3+G4 cumulative `git diff 2b5e6dd..0d02c47` (4 commits, 809 lines): `src/app/schemas.py` + `src/app/orchestration.py` + `src/app/main.py` + `src/app/dependencies.py` + `src/app/routes/__init__.py` + `src/app/routes/health.py` + `pyproject.toml` (httpx dev dep) + 4 new unit-test files | minor-fixes-recommended → clean after fix | 0 P0, 0 P1, 0 P2, 4 P3, 1 info | Codex unblocked (Bucket 5 trigger fired). **All P3s + info closed inline at `e08fca5`.** G-G1G4-001 (P3, convention) — three `_probe` inner functions in `test_app_main.py` missing return-type annotations: closed by adding `-> dict[str, bool]` and typed `Depends()` parameters. G-G1G4-002 (P3, convention) — three `boom()` stubs in `test_app_orchestration.py` missing `(*args: object, **kwargs: object) -> None`: closed by tightening signatures. G-G1G4-003 (P3, convention) — unused `import os` in `test_app_main.py`: removed. G-G1G4-004 (P3, DI-lifespan) — startup-error robustness: if a future startup step raises after `app.state.engine = engine`, dispose was never called because the `try:` block began only at `yield`. Closed by capturing the local `engine` reference before the try and disposing in `finally` based on the local rather than re-reading from `app.state`. G-G1G4-005 (P3, test-fragility) — no dedicated test for the validator-`partial` path through `run_dsl_query()`: closed by adding `TestPartialValidationPath` (1 test stubbing `validate` to return status='partial' with one warning, asserting findings thread through to the response envelope). G-G1G4-006 (info, test-fragility) — health-test fixture deliberately doesn't use `TestClient` as a context manager so lifespan isn't triggered (because /health is liveness-only): closed by adding an explanatory comment to the fixture. 374 unit tests green after closures. |
| 2026-05-09 | [Codex code (Slice G close)](../reviews/review-codex-code-slice-g-close-2026-05-09.md) | Slice G close — full slice diff `2b5e6dd..0c85848` (8 commits): `src/app/` foundation (schemas, orchestration, main, dependencies, routes/__init__, routes/health, routes/query) + canonical-09 §1 amendment + `pyproject.toml` httpx dev dep + 5 new test files (test_app_schemas.py, test_app_orchestration.py, test_app_main.py, test_app_health.py, test_app_routes.py, test_app_dsl_route.py). The full FastAPI surface for `POST /api/v1/query/dsl`, `GET /api/v1/health`, plus error-mapping table + DEC-G1..G14 → DEC-062..066. | minor-fixes-recommended → clean after fix | 0 P0, 0 P1, 0 P2, 1 P3, 0 info | **First real Codex pass on the repo** since Slices E + F shipped on Claude-fallback reviews. **Bucket 5 ✅ CLOSED** by this review artifact. **Single P3 closed inline at `bf3b878`.** G-CLOSE-001 (P3, convention/ruff) — unused `ValidationUnsupported` import in `tests/unit/test_app_routes.py`: removed; `ruff check src/app tests/unit/test_app_*.py tests/integration/test_app_dsl_route.py` reports all checks pass. The mid-slice checkpoint's 4 P3 + 1 info closures were re-verified at slice close. **Slice G exit gate**: `test_query_dsl_returns_flagship_sequence_envelope` in `tests/integration/test_app_dsl_route.py` — a real `POST /api/v1/query/dsl {"dsl": "faith > hope > love"}` against the live corpus returns HTTP 200 + a JSON envelope with 2 candidates at 1Cor 13:13, baselines 483/84/259, 6 alternative-orderings, `null_distribution: null` (DEC-065 dump-shape verification), and `explanation.summary` ≤ 5 lines. Plus four sad-path live-DB integration assertions (parse_error/validation_unsupported/concept_not_mapped/health). Total test counts at close: **387 unit + 75 integration = 462 tests passing in 31s** (was 340 unit + 70 integration = 410 at Slice F close; +47 unit + 5 integration = +52 net). **Closing SHA chain**: `281afaf` (G1 schemas) → `c46ca65` (G2 orchestration helper) → `5fe4bd7` (G3 factory + lifespan + DI) → `0d02c47` (G4 health route) → `e08fca5` (G4b mid-slice closures) → `37c1cda` (G5 DSL route + exception mapping) → `4ef1217` (G6 live integration exit gate) → `0c85848` (G7 canonical-09 §1 amendment) → `bf3b878` (G-close P3 closure) → `<governance-SHA>` (slice-close governance: DEC-062..066, spec-coverage rows for REQ:09.api-gateway + REQ:09.request-lifecycle, this row, project_status memory). DEC-062..066 cover the load-bearing decisions. **OQ #6 ✅ resolved as DEC-065** (emit nullable fields as JSON null; default Pydantic v2 behavior; symmetric with project-wide pattern). **OQ #5 ✅ resolved**: REQ:01.transparent-evidence requires no canonical-01 amendment — the explainer prose + contextualization envelope satisfy "show the generated query, explain why results matched, label match types distinctly" surface-agnostically (CLI in Slice F, HTTP in Slice G). |
| 2026-05-09 | [Claude code, fallback (Slice H H1+H2 checkpoint)](../reviews/review-claude-code-slice-h-h1h2-checkpoint-2026-05-09.md) | Slice H mid-slice — H1+H2 cumulative `git diff da0b4c9..18a8d1a` (2 commits, ~580 lines): `src/nlp/llm_client.py` (NEW LLMClient + AnthropicLLMClient + LLMUnavailable + build_anthropic_client_from_env) + `src/nlp/translator.py` (NEW translate + TranslationContext + TranslationResult + NLCompileError + 5 line-anchored regex helpers) + `src/nlp/prompts/system_prompt.py` (NEW SYSTEM_PROMPT + build_system_prompt) + `pyproject.toml` (anthropic>=0.40,<1.0 + live_llm marker) + `src/nlp/explainer.py` (drive-by ruff cleanup) + 32 new unit tests. Codex blocked by `~/.codex/sessions` permission issue at this checkpoint; Claude `general-purpose` fallback ran the same severity language and checklist. | minor-fixes-recommended → clean after fix | 0 P0, 0 P1, 1 P2, 5 P3, 2 info | **All P0/P1/P2 closed inline at `b0f097f`.** **H-H1H2-001 (P2)** — bare `except anthropic.APIError` in AnthropicLLMClient.complete() wrapped 4xx code-bug subclasses (BadRequestError, NotFoundError, UnprocessableEntityError, ConflictError) as 503 LLMUnavailable, conflating availability faults with translator-side request bugs. Per DEC-070's design contract — 5xx means "system fault, not your fault" — 4xx errors must propagate raw so the route returns 500 internal_error rather than 503 llm_unavailable. **Fix**: narrow the except chain to the actual availability + auth + 5xx-server exceptions: APIConnectionError, APITimeoutError, RateLimitError, AuthenticationError, PermissionDeniedError, InternalServerError. All other APIError subclasses propagate. Six new tests lock the behavior — two for newly-wrapped classes (InternalServerError, PermissionDeniedError) and a parameterized set for the four 4xx classes that must propagate raw. **P3 closures bundled at `b0f097f`**: TranslationResult.alternatives uses `Field(default_factory=list)` instead of mutable bare default `[]`; module-level docstring on src/nlp/llm_client.py adds security note warning future authors not to log `vars(self._client)` (anthropic SDK exposes api_key publicly on the instance). **P3 deferred to slice close**: "Confidence defaults to 1.0 on parse failure" — addressed at H-CLOSE-003 below. **P3 deferred to H4**: widen except chain audit when route's exception mapping lands — verified consistent at H4. |
| 2026-05-09 | [Codex code (Slice H H3+H4 checkpoint)](../reviews/review-codex-code-slice-h-h3h4-checkpoint-2026-05-09.md) | Slice H mid-slice — H3+H4 cumulative `git diff 4448d59..0dda204` (2 commits, ~610 lines): `src/app/schemas.py` (TranslationMetadata + QueryNLRequest + QueryNLResponse subclassing QueryDSLResponse) + `src/app/orchestration.py` (run_nl_query) + `src/app/dependencies.py` (get_llm_client + get_translation_context) + `src/app/main.py` (lifespan reads ANTHROPIC_API_KEY independently; mounts new router) + `src/app/routes/nl.py` (NEW POST /api/v1/query/nl handler with 8-branch exception mapping) + 23 new unit tests across 4 files. **Codex unblocked** (~/.codex permissions intact for this attempt). | minor-fixes-recommended → clean after fix | 0 P0, 0 P1, 0 P2, 1 P3, 0 info | **Single P3 closed inline at `e289499`.** **H-H3H4-001 (P3, contract clarity)** — the lifespan docstring said DATABASE_URL and ANTHROPIC_API_KEY "degrade independently" without specifying whether that covered env-var absence only or also covered construction failures. Implementation matches "absence only" — construction errors are fail-fast so deployment problems aren't masked behind runtime 503. Updated the module docstring to make the contract explicit. **Codex's noted test-coverage gap closed**: NL route's exception-mapping test class lacked direct unit tests for ParseError and ValidationUnsupported (those flow through run_nl_query but were never exercised at the route layer). Added 2 tests using the same monkeypatch.setattr("src.app.routes.nl.run_nl_query", boom) pattern. **All 6 focus categories clean**: HTTP error mapping completeness; DI symmetry with Slice G; schema subclassing (Pydantic v2 inheritance pitfalls); exception-chain ordering across orchestration boundary; lifespan independent-degradation semantics; security (no new vars()/logging leak paths). |
| 2026-05-09 | [Codex code (Slice I mid-slice)](../reviews/review-codex-code-slice-i-mid-checkpoint-2026-05-09.md) | Slice I mid-slice — I1+I2+I3 cumulative `git diff 5ddbf40..88556be` (3 commits, ~750 lines): `src/app/routes/{capabilities, concepts, validate}.py` (NEW); `src/app/{main, schemas, orchestration}.py` (MODIFIED); `src/ontology/registry.py` (MODIFIED — ConceptSummary + list_all_concepts); 20 new unit tests across 3 new test files + 5 added across 2 modified files. | minor-fixes-recommended → clean after fix | 0 P0, 0 P1, 1 P2, 0 P3, 0 info | **I-MID-001 (P2) closed inline at `786ac6c`.** The only `list_all_concepts()` test exercised the empty-registry short-circuit (`engine=None → []`); the SQL path at src/ontology/registry.py:363-407 was never hit at unit level. Future regressions changing LEFT JOIN to INNER JOIN, pushing the language filter into WHERE, or dropping concepts after filtering would not be caught. **Fix**: 4 new table-backed unit tests using the MagicMock chaining pattern from tests/unit/test_contextualization.py — TestListAllConceptsSqlPath covers (a) concept with no JOINed lemma rows surfaces with lemmas=[]; (b) multi-language lemmas filtered by language=grc drops heb lemma without dropping the concept; (c) language filter matching no rows keeps the concept with lemmas=[] (forward-compat invariant); (d) multi-concept aggregation preserves alphabetical ordering and per-concept lemma lists. **Three Codex-verified strengths**: DEC-079 contract holds (validate has exactly one 422 branch — ParseError only); TestValidateUnsupportedReturnsTwoHundredNotFourTwentyTwo is load-bearing as a regression guard; no circular imports between `src/app/schemas.py` ↔ `src/ontology/registry.py`. |
| 2026-05-09 | [Codex code (Slice I close)](../reviews/review-codex-code-slice-i-close-2026-05-09.md) | Slice I close — full slice cumulative `git diff 5ddbf40..95f9eab` (5 commits, ~1,100 lines): `src/app/routes/{capabilities, concepts, validate}.py` + 3 helpers (run_validate_only, list_all_concepts, ConceptSummary) + canonical-09 §1 routes-table amendment marking all six MVP routes as shipped + 25 new unit tests + 5 new integration tests. The remaining three MVP routes from canonical-09 §1 (capabilities, concepts, query/validate) shipping closes the §1 routes-table promise opened in Slice G. | clean | 0 P0, 0 P1, 0 P2, 0 P3, 0 info | **No findings at any severity.** Mid-slice closure (I-MID-001) verified held in final state. End-to-end correctness traced through DI providers → handler → orchestration helper → response envelope for all three new routes. No Slice G/H regressions. DEC-079 contract triple-locked (route try/except chain, unit regression guard, live integration test). Schema serialization verified: `CapabilityRegistry` direct re-use round-trips; `ConceptsResponse` and `QueryValidateResponse` frozen invariants hold; `QueryValidateResponse` correctly omits `result`/`explanation`. **Slice I exit gate**: `tests/integration/test_app_endpoint_followup.py` — 5 integration tests (capabilities returns MVP body; concepts returns seeded faith/hope/love with verification_state + lemmas; validate happy path returns 200; validate parse_error returns 422; validate unsupported returns 200 with `validation.status='unsupported'`). NOT yet executed in this orchestrator session (no DATABASE_URL set); test collects cleanly via `pytest --collect-only`. **Bucket 9 NEW** (concepts pagination). Codex-recommended sharpened trigger: *"When `concept_lemmas` exceeds 500 rows OR any /api/v1/concepts response exceeds 50 KB."* Concrete + measurable; bottleneck is lemma-row count, not concept-row count. **Buckets 6, 7, 8 re-deferred unchanged** — Slice I touched none of their triggers. **Test counts at close**: 485 unit + 80 integration + 2 live_llm = 567 tests total. Was 529 at Slice H close; +56 unit + 5 integration net for Slice I. Default `pytest` runs in <1.2s; ruff clean. **Closing SHA chain**: `d5e7681` (I1 capabilities) → `43a2ca2` (I2 concepts + list_all_concepts) → `88556be` (I3 validate + run_validate_only) → `786ac6c` (I3b mid-slice review closure) → `95f9eab` (I4 integration exit gate) → `<governance-SHA>` (I5 canonical-09 + spec-coverage + reviews-log + decision-log + project_status memory). **DEC-075..080** cover the slice's load-bearing decisions. **OQ resolutions**: OQ-I1 → DEC-075 (no envelope); OQ-I2 → DEC-076 (flat list, no pagination at MVP); OQ-I3 → DEC-078 (QueryValidateResponse envelope); OQ-I4 → DEC-079 (200 on unsupported); OQ-I5 → Bucket 9 with sharpened trigger; OQ-I6 (cache headers) → deferred / not pursued; OQ-I7 (language query param) → resolved at I2 (param exists, defaults to grc, propagates correctly per TestConceptsRouteLanguageQuery). |
| 2026-05-09 | [Codex code (Slice H close)](../reviews/review-codex-code-slice-h-close-2026-05-09.md) | Slice H close — full slice cumulative `git diff da0b4c9..43a71e6` (8 commits, ~2,400 lines): `src/nlp/llm_client.py` + `src/nlp/translator.py` + `src/nlp/prompts/system_prompt.py` + `src/app/{schemas, orchestration, dependencies, main, routes/nl}.py` + canonical-09 §1+§2 amendments + 65 new unit tests + 2 live_llm integration tests + 2 mid-slice review artifacts. The full NL→DSL surface for `POST /api/v1/query/nl`, plus the project's first LLM dependency (anthropic>=0.40,<1.0). | minor-fixes-recommended → clean after fix | 0 P0, 0 P1, 0 P2, 3 P3, 1 info | **Mid-slice closures verified held in final state.** **H-CLOSE-002 (P3)** — `nl_query` field on `QueryNLRequest` had no `max_length`; very long inputs would propagate to the LLM unchecked. **Closed inline**: added `max_length=2000` (chosen as ~10× the longest realistic research question; bounds adversarial / accidentally-pasted-prose inputs without rejecting normal usage). 2 new schema tests lock the behavior. **H-CLOSE-003 (P3)** — `_extract_confidence` defaulted to `1.0` (max confidence) when the LLM's `Confidence:` line was missing or malformed. Conflicts with DEC-024 corpus-is-ground-truth charter (don't claim certainty the LLM didn't claim). **Closed inline**: changed both the parser fallback AND the `TranslationResult.confidence` Pydantic default to `0.0`. Updated 3 affected tests (renamed from `*_defaults_to_one` to `*_defaults_to_zero`). DEC-072 still holds (confidence is informational, not control); the calibration of the value itself is a transparency claim. **H-CLOSE-001 (P3) bucketed → Bucket 8 NEW**. Static system-prompt context: `TranslationContext` is built from a snapshot of the registry at startup with no invalidation mechanism. **Trigger**: when a registry curator workflow lands OR when `verification_state` updates mid-process and stale system prompts cause user-visible incorrect translations. **Rationale**: Slice H's static cookbook is a deliberate design choice (DEC-071) and the registry is stable today; runtime invalidation is premature and would expand scope. Re-defer with this specific trigger; not "eventually." **Slice H exit gate**: `tests/integration/test_app_nl_route_live_llm.py::test_nl_query_compiles_and_returns_flagship_envelope` exists with shape-correct assertions but **was NOT executed in this orchestrator session** because neither `DATABASE_URL` nor `ANTHROPIC_API_KEY` were set. Test collects cleanly via `pytest --collect-only`; live verification is deferred to user with both env vars exported. Per project transparency rule (CLAUDE.md): "the system must say when it cannot do something yet." **Bucket 6 ⚠ RE-OPENED** — pre-slice-start decision to "close formally" is reversed. Codex review correctly flagged that Bucket 6's purpose (end-to-end cookbook verification against a real LLM agent) has not yet been satisfied; Slice G's HTTP integration tests do not exercise the cookbook content (the cookbook is consumed by the NL route's translator, not by the DSL route). **New trigger**: "next session in which both `DATABASE_URL` and `ANTHROPIC_API_KEY` are set and `pytest -m live_llm tests/integration/test_app_nl_route_live_llm.py` runs to completion." Slice H's live-LLM exit gate IS the closing test for Bucket 6; the test file exists, just hasn't fired. **Bucket 7 ⏳ re-deferred** with sharpened trigger per pre-slice-start plan: "after Slice H ships AND user has run ≥1 real NL query through `POST /api/v1/query/nl` AND either (a) judged the deterministic explainer prose inadequate OR (b) explicitly authorized wiring the LLM client into the explainer's conceptual-match path." DEC-061 framing held — kitchen-sink slice avoided. **Bucket 8 NEW**: see H-CLOSE-001 above. **Test counts at close**: 452 unit + 75 integration + 2 live_llm (deselected by default) = 529 tests total. Was 462 at Slice G close; +63 unit + 2 live_llm = +65 net for Slice H. Default `pytest` runs in <1.5s. **Closing SHA chain**: `3c8afda` (H1 LLMClient seam) → `18a8d1a` (H2 translator + system prompt) → `b0f097f` (H2b H-H1H2 closures) → `4448d59` (H3 schemas + run_nl_query) → `0dda204` (H4 route + DI providers) → `e289499` (H4b H-H3H4 closures) → `4b061fa` (H5 live-LLM exit-gate test) → `43a71e6` (H6 canonical-09 + spec-coverage) → `<governance-SHA>` (slice-close governance: DEC-067..074, spec-coverage REQ:09.nl-to-dsl row, this row, project_status memory). DEC-067..074 cover the slice's load-bearing decisions. **OQ-H1 (cookbook embedding sufficiency)**: deferred — needs real NL queries to evaluate; closes when the live-LLM exit gate runs. **OQ-H2 (free-form vs structured output)**: resolved at H2 — free-form with structured `DSL: <string>` line + line-anchored regex extraction. **OQ-H3 (LLM-in-explainer)**: re-deferred as Bucket 7 (above). **OQ-H4 (confidence/alternatives required vs optional)**: resolved at H-CLOSE-003 — required fields with honest defaults (0.0 / [] / "") rather than overconfident fallbacks. **OQ-H5 (Bucket 6 closure)**: resolved at this slice close — re-deferred with sharpened trigger (Bucket 6 above). |

## Severity language

Code reviews use P-numbers. Adversarial design reviews use high / medium / low.

| Severity | Meaning | Disposition |
|----------|---------|-------------|
| P0 | No-ship — blocks merge | Must close before the slice closes |
| P1 | Must-fix-before-merge | Must close before the slice closes |
| P2 | Must-fix-before-slice-close | Must close before the slice closes |
| P3 | Worth-doing-soon | Fix inline or schedule into a named bucket |
| info | Note for future authors | Acknowledged in the closure column |
| high (design) | Architectural risk; treat as P0/P1 | Close inline or land in a tracked bucket with a named trigger |
| medium (design) | Worth addressing | Treat as P3 — fix soon or bucket |
| low (design) | Note | Acknowledge |

## Process

- **When (slice-close pass)**: After the slice's last `/review`, before its `/close-step`. The Codex pass runs on the cumulative slice diff (`git diff <pre-slice-base>..HEAD`).
- **How**: User invokes `/codex:rescue` or the assistant spawns the `codex:codex-rescue` subagent with a self-contained brief naming scope, prior-review artifacts (so severity language calibrates), severity language, and the categories to focus on (correctness / security / resource hygiene / test fragility / contract / convention / subprocess-env).
- **Where the artifact lands**: `docs/reviews/review-codex-{code|adversarial-design}-{slice-id}-{YYYY-MM-DD}.md`. Include frontmatter with type, verdict, base SHA, scope, plugin/CLI versions, and findings summary.
- **Closure dispositions** (every finding gets exactly one):
  - **fixed** — fix-commit SHA recorded in this log.
  - **deferred to a tracked bucket** — bucket name + trigger condition + written rationale (why this slice is not the right home). The trigger MUST be specific enough that a future-you can recognize the matching slice without re-reading the original finding.
  - **rejected** — documented reason; rare.
  - "Filed and forgotten" is **not** a valid disposition.
- **Buckets**: Findings deferred to a later slice are tracked as named buckets ("Bucket 1", "Bucket 2", etc.). The closure column here carries the trigger + rationale; in-flight buckets are also summarized in `project_status.md`'s Carry-over memory; closed buckets dissolve into the SHA in the closure column.
- **Adversarial design reviews** are a separate flavor: run them when an architectural decision lands or before a major slice begins. Findings that close inline get a DEC; findings deferred join a bucket with a stated trigger and rationale, same as code findings.

## Between-slice triage

When a new slice begins, the assistant MUST do a pre-slice triage pass **before invoking `/research`**. This is the mirror of the slice-close review and the mechanism that prevents bucketed findings from drifting indefinitely.

1. **Scan**: read this file's table for any bucket whose **trigger** condition fires on the new slice (e.g. "Slice C blockers", "pre-pattern-engine", "when REQ:NN.X is scheduled"). Cross-reference `project_status.md` Carry-over for active triggers.
2. **Disposition**: every matching bucket gets one of three explicit choices:
   - **Scope in** — fold the items into this slice's `/research` and `/design`. Note in the design discussion which findings are now in scope. Update the bucket's closure column with `scoped into <slice-id>; see <design artifact>` and (later, after slice close) the closing SHA.
   - **Re-defer** — update the bucket entry with a new trigger and a written rationale (why this slice is still not the right home). Stale "eventually" deferrals are not allowed; re-deferral requires a new specific trigger.
   - **Reject** — document why the original finding no longer applies (rare; finding may have been overtaken by other work).
3. **Surface**: any matching bucket the assistant cannot dispositionalize (because the user hasn't decided yet) is reported to the user **before** `/research` runs, not after.
4. **Trigger quality**: a trigger should answer "what slice or pre-slice condition closes this?" Specific examples:
   - ✅ "Before any pattern-engine work that consumes the DSL `book:` constraint"
   - ✅ "When epistemic-discipline backlog items 3–5 are scheduled"
   - ✅ "When canonical-08 REQ:08.registry-epistemics is added"
   - ❌ "Eventually" / "When we get to it" / "Future" — these are not triggers.

This protocol is what makes "deliberately scoped or re-deferred with rationale" a process rule, not a hope.

## Cross-references

- Quality Gate definition: `CLAUDE.md` § Quality Gates and § Workflow step 6 (slice close).
- Decision log (DECs that close findings): `docs/governance/decision-log.md`.
- Spec coverage (REQ markers ↔ code/test): `docs/governance/spec-coverage.md`.
- Bucket-tracking memory (in-flight buckets only): user's `project_status.md` memory file.
